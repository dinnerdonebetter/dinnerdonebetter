# Mount GCP Secret Manager secrets via CSI driver
# This patch adds the service account and CSI volume to deployments
- op: add
  path: "/spec/template/spec/serviceAccountName"
  value: api-service-account
- op: add
  path: "/spec/template/spec/volumes/-"
  value:
    name: secrets-store
    csi:
      driver: secrets-store-gke.csi.k8s.io
      readOnly: true
      volumeAttributes:
        secretProviderClass: api-secrets
- op: add
  path: "/spec/template/spec/containers/0/volumeMounts/-"
  value:
    name: secrets-store
    mountPath: "/mnt/secrets"
    readOnly: true
