# Workload identity for CronJobs - use api-service-account for GCP Pub/Sub, etc.
# CronJobs get credentials from api-service-config (Terraform K8s secret), not CSI/Secret Manager.
- op: add
  path: "/spec/jobTemplate/spec/template/spec/serviceAccountName"
  value: api-service-account
