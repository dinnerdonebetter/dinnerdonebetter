---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dinner-done-better-api-ingress
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: "gce"
    external-dns.alpha.kubernetes.io/hostname: "api.dinnerdonebetter.dev"
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dinner-done-better-api-svc
                port:
                  number: 8000
---
