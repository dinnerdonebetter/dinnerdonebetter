---
on:
  pull_request:
    paths:
      - frontend/**

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

name: frontend build
jobs:
  frontend:
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Build
        uses: dagger/dagger-for-github@v8
        with:
          version: "0.18.19"
          verb: call
          module: ./dagger
          args: frontend-build --source=.
