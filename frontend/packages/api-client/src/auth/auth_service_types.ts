// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.1
// source: auth/auth_service_types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { ResponseDetails } from '../common';
import { Pagination, QueryFilter } from '../filtering';
import { Timestamp } from '../google/protobuf/timestamp';
import { Account, User } from '../identity/identity_messages';
import { UserLoginInput, UserSession } from './auth_messages';

export const protobufPackage = 'auth';

export interface LoginForTokenRequest {
  input: UserLoginInput | undefined;
}

export interface LoginForTokenResponse {
  responseDetails: ResponseDetails | undefined;
  result: TokenResponse | undefined;
}

export interface AdminLoginForTokenRequest {
  input: UserLoginInput | undefined;
}

export interface AdminLoginForTokenResponse {
  responseDetails: ResponseDetails | undefined;
  result: TokenResponse | undefined;
}

export interface ExchangeTokenRequest {
  refreshToken: string;
  desiredAccountId: string;
}

export interface ExchangeTokenResponse {
  responseDetails: ResponseDetails | undefined;
  userId: string;
  accountId: string;
  accessToken: string;
  refreshToken: string;
  expiresUtc: Date | undefined;
}

export interface GetActiveAccountRequest {}

export interface GetActiveAccountResponse {
  responseDetails: ResponseDetails | undefined;
  result: Account | undefined;
}

export interface GetAuthStatusRequest {}

export interface GetAuthStatusResponse {
  responseDetails: ResponseDetails | undefined;
  userId: string;
  accountStatus: string;
  accountStatusExplanation: string;
  activeAccount: string;
  requiresPasswordChange: boolean;
}

export interface RedeemPasswordResetTokenRequest {
  token: string;
  newPassword: string;
}

export interface RedeemPasswordResetTokenResponse {
  responseDetails: ResponseDetails | undefined;
}

export interface RefreshTOTPSecretRequest {
  currentPassword: string;
  totpToken: string;
}

export interface RefreshTOTPSecretResponse {
  responseDetails: ResponseDetails | undefined;
  result: TOTPSecretRefreshResponse | undefined;
}

export interface RequestEmailVerificationEmailRequest {
  emailAddress: string;
}

export interface RequestEmailVerificationEmailResponse {
  responseDetails: ResponseDetails | undefined;
  submitted: boolean;
}

export interface RequestPasswordResetTokenRequest {
  emailAddress: string;
}

export interface RequestPasswordResetTokenResponse {
  responseDetails: ResponseDetails | undefined;
  accepted: boolean;
}

export interface RequestUsernameReminderRequest {
  emailAddress: string;
}

export interface RequestUsernameReminderResponse {
  responseDetails: ResponseDetails | undefined;
  submitted: boolean;
}

export interface UpdatePasswordRequest {
  newPassword: string;
  currentPassword: string;
  totpToken: string;
}

export interface UpdatePasswordResponse {
  responseDetails: ResponseDetails | undefined;
}

export interface VerifyEmailAddressRequest {
  token: string;
}

export interface VerifyEmailAddressResponse {
  responseDetails: ResponseDetails | undefined;
  verified: boolean;
}

export interface VerifyTOTPSecretRequest {
  totpToken: string;
  userId: string;
}

export interface VerifyTOTPSecretResponse {
  responseDetails: ResponseDetails | undefined;
  accepted: boolean;
}

export interface EmailAddressVerificationRequest {
  token: string;
}

export interface EmailAddressVerificationResponse {
  responseDetails: ResponseDetails | undefined;
}

export interface PasswordResetResponse {
  responseDetails: ResponseDetails | undefined;
  successful: boolean;
}

export interface PasswordResetTokenCreationRequestInput {
  emailAddress: string;
}

export interface PasswordResetTokenRedemptionRequestInput {
  token: string;
  newPassword: string;
}

export interface TOTPSecretRefreshResponse {
  twoFactorQrCode: string;
  twoFactorSecret: string;
}

export interface TOTPSecretVerificationResponse {
  responseDetails: ResponseDetails | undefined;
  accepted: boolean;
}

export interface TokenResponse {
  userId: string;
  accountId: string;
  accessToken: string;
  refreshToken: string;
  expiresUtc: Date | undefined;
}

export interface UserPermissionsRequestInput {
  permissions: string[];
}

export interface UserPermissionsResponse {
  responseDetails: ResponseDetails | undefined;
  permissions: { [key: string]: boolean };
}

export interface UserPermissionsResponse_PermissionsEntry {
  key: string;
  value: boolean;
}

export interface UsernameReminderRequestInput {
  emailAddress: string;
}

export interface EvaluateBooleanFeatureFlagRequest {
  featureFlag: string;
}

export interface EvaluateBooleanFeatureFlagResponse {
  responseDetails: ResponseDetails | undefined;
  enabled: boolean;
}

export interface EvaluateInt64FeatureFlagRequest {
  featureFlag: string;
}

export interface EvaluateInt64FeatureFlagResponse {
  responseDetails: ResponseDetails | undefined;
  value: number;
}

export interface EvaluateStringFeatureFlagRequest {
  featureFlag: string;
}

export interface EvaluateStringFeatureFlagResponse {
  responseDetails: ResponseDetails | undefined;
  value: string;
}

export interface BeginPasskeyRegistrationRequest {}

export interface BeginPasskeyRegistrationResponse {
  responseDetails: ResponseDetails | undefined;
  publicKeyCredentialCreationOptions: Uint8Array;
  challenge: string;
}

export interface FinishPasskeyRegistrationRequest {
  attestationResponse: Uint8Array;
  challenge: string;
}

export interface FinishPasskeyRegistrationResponse {
  responseDetails: ResponseDetails | undefined;
}

export interface BeginPasskeyAuthenticationRequest {
  username: string;
}

export interface BeginPasskeyAuthenticationResponse {
  responseDetails: ResponseDetails | undefined;
  publicKeyCredentialRequestOptions: Uint8Array;
  challenge: string;
}

export interface FinishPasskeyAuthenticationRequest {
  assertionResponse: Uint8Array;
  challenge: string;
  username: string;
}

export interface GetSelfRequest {}

export interface GetSelfResponse {
  responseDetails: ResponseDetails | undefined;
  result: User | undefined;
}

export interface ListPasskeysRequest {}

export interface ListPasskeysResponse {
  responseDetails: ResponseDetails | undefined;
  results: PasskeyCredential[];
}

export interface PasskeyCredential {
  id: string;
  friendlyName: string;
  createdAt: Date | undefined;
  lastUsedAt: Date | undefined;
}

export interface ArchivePasskeyRequest {
  credentialId: string;
}

export interface ArchivePasskeyResponse {
  responseDetails: ResponseDetails | undefined;
}

export interface ListActiveSessionsRequest {
  filter: QueryFilter | undefined;
}

export interface ListActiveSessionsResponse {
  responseDetails: ResponseDetails | undefined;
  pagination: Pagination | undefined;
  sessions: UserSession[];
}

export interface RevokeSessionRequest {
  sessionId: string;
}

export interface RevokeSessionResponse {
  responseDetails: ResponseDetails | undefined;
}

export interface RevokeAllOtherSessionsRequest {}

export interface RevokeAllOtherSessionsResponse {
  responseDetails: ResponseDetails | undefined;
}

export interface RevokeCurrentSessionRequest {}

export interface RevokeCurrentSessionResponse {
  responseDetails: ResponseDetails | undefined;
}

export interface AdminListSessionsForUserRequest {
  userId: string;
  filter: QueryFilter | undefined;
}

export interface AdminRevokeUserSessionRequest {
  userId: string;
  sessionId: string;
}

export interface AdminRevokeAllUserSessionsRequest {
  userId: string;
}

function createBaseLoginForTokenRequest(): LoginForTokenRequest {
  return { input: undefined };
}

export const LoginForTokenRequest: MessageFns<LoginForTokenRequest> = {
  encode(message: LoginForTokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.input !== undefined) {
      UserLoginInput.encode(message.input, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoginForTokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoginForTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.input = UserLoginInput.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoginForTokenRequest {
    return { input: isSet(object.input) ? UserLoginInput.fromJSON(object.input) : undefined };
  },

  toJSON(message: LoginForTokenRequest): unknown {
    const obj: any = {};
    if (message.input !== undefined) {
      obj.input = UserLoginInput.toJSON(message.input);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LoginForTokenRequest>, I>>(base?: I): LoginForTokenRequest {
    return LoginForTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LoginForTokenRequest>, I>>(object: I): LoginForTokenRequest {
    const message = createBaseLoginForTokenRequest();
    message.input =
      object.input !== undefined && object.input !== null ? UserLoginInput.fromPartial(object.input) : undefined;
    return message;
  },
};

function createBaseLoginForTokenResponse(): LoginForTokenResponse {
  return { responseDetails: undefined, result: undefined };
}

export const LoginForTokenResponse: MessageFns<LoginForTokenResponse> = {
  encode(message: LoginForTokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.result !== undefined) {
      TokenResponse.encode(message.result, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoginForTokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoginForTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.result = TokenResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoginForTokenResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      result: isSet(object.result) ? TokenResponse.fromJSON(object.result) : undefined,
    };
  },

  toJSON(message: LoginForTokenResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.result !== undefined) {
      obj.result = TokenResponse.toJSON(message.result);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LoginForTokenResponse>, I>>(base?: I): LoginForTokenResponse {
    return LoginForTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LoginForTokenResponse>, I>>(object: I): LoginForTokenResponse {
    const message = createBaseLoginForTokenResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.result =
      object.result !== undefined && object.result !== null ? TokenResponse.fromPartial(object.result) : undefined;
    return message;
  },
};

function createBaseAdminLoginForTokenRequest(): AdminLoginForTokenRequest {
  return { input: undefined };
}

export const AdminLoginForTokenRequest: MessageFns<AdminLoginForTokenRequest> = {
  encode(message: AdminLoginForTokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.input !== undefined) {
      UserLoginInput.encode(message.input, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AdminLoginForTokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAdminLoginForTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.input = UserLoginInput.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AdminLoginForTokenRequest {
    return { input: isSet(object.input) ? UserLoginInput.fromJSON(object.input) : undefined };
  },

  toJSON(message: AdminLoginForTokenRequest): unknown {
    const obj: any = {};
    if (message.input !== undefined) {
      obj.input = UserLoginInput.toJSON(message.input);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AdminLoginForTokenRequest>, I>>(base?: I): AdminLoginForTokenRequest {
    return AdminLoginForTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AdminLoginForTokenRequest>, I>>(object: I): AdminLoginForTokenRequest {
    const message = createBaseAdminLoginForTokenRequest();
    message.input =
      object.input !== undefined && object.input !== null ? UserLoginInput.fromPartial(object.input) : undefined;
    return message;
  },
};

function createBaseAdminLoginForTokenResponse(): AdminLoginForTokenResponse {
  return { responseDetails: undefined, result: undefined };
}

export const AdminLoginForTokenResponse: MessageFns<AdminLoginForTokenResponse> = {
  encode(message: AdminLoginForTokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.result !== undefined) {
      TokenResponse.encode(message.result, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AdminLoginForTokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAdminLoginForTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.result = TokenResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AdminLoginForTokenResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      result: isSet(object.result) ? TokenResponse.fromJSON(object.result) : undefined,
    };
  },

  toJSON(message: AdminLoginForTokenResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.result !== undefined) {
      obj.result = TokenResponse.toJSON(message.result);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AdminLoginForTokenResponse>, I>>(base?: I): AdminLoginForTokenResponse {
    return AdminLoginForTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AdminLoginForTokenResponse>, I>>(object: I): AdminLoginForTokenResponse {
    const message = createBaseAdminLoginForTokenResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.result =
      object.result !== undefined && object.result !== null ? TokenResponse.fromPartial(object.result) : undefined;
    return message;
  },
};

function createBaseExchangeTokenRequest(): ExchangeTokenRequest {
  return { refreshToken: '', desiredAccountId: '' };
}

export const ExchangeTokenRequest: MessageFns<ExchangeTokenRequest> = {
  encode(message: ExchangeTokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.refreshToken !== '') {
      writer.uint32(10).string(message.refreshToken);
    }
    if (message.desiredAccountId !== '') {
      writer.uint32(18).string(message.desiredAccountId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExchangeTokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExchangeTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.refreshToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.desiredAccountId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExchangeTokenRequest {
    return {
      refreshToken: isSet(object.refreshToken)
        ? globalThis.String(object.refreshToken)
        : isSet(object.refresh_token)
          ? globalThis.String(object.refresh_token)
          : '',
      desiredAccountId: isSet(object.desiredAccountId)
        ? globalThis.String(object.desiredAccountId)
        : isSet(object.desired_account_id)
          ? globalThis.String(object.desired_account_id)
          : '',
    };
  },

  toJSON(message: ExchangeTokenRequest): unknown {
    const obj: any = {};
    if (message.refreshToken !== '') {
      obj.refreshToken = message.refreshToken;
    }
    if (message.desiredAccountId !== '') {
      obj.desiredAccountId = message.desiredAccountId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExchangeTokenRequest>, I>>(base?: I): ExchangeTokenRequest {
    return ExchangeTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExchangeTokenRequest>, I>>(object: I): ExchangeTokenRequest {
    const message = createBaseExchangeTokenRequest();
    message.refreshToken = object.refreshToken ?? '';
    message.desiredAccountId = object.desiredAccountId ?? '';
    return message;
  },
};

function createBaseExchangeTokenResponse(): ExchangeTokenResponse {
  return {
    responseDetails: undefined,
    userId: '',
    accountId: '',
    accessToken: '',
    refreshToken: '',
    expiresUtc: undefined,
  };
}

export const ExchangeTokenResponse: MessageFns<ExchangeTokenResponse> = {
  encode(message: ExchangeTokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.userId !== '') {
      writer.uint32(18).string(message.userId);
    }
    if (message.accountId !== '') {
      writer.uint32(26).string(message.accountId);
    }
    if (message.accessToken !== '') {
      writer.uint32(34).string(message.accessToken);
    }
    if (message.refreshToken !== '') {
      writer.uint32(42).string(message.refreshToken);
    }
    if (message.expiresUtc !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresUtc), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExchangeTokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExchangeTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.accessToken = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.refreshToken = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.expiresUtc = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExchangeTokenResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
      accountId: isSet(object.accountId)
        ? globalThis.String(object.accountId)
        : isSet(object.account_id)
          ? globalThis.String(object.account_id)
          : '',
      accessToken: isSet(object.accessToken)
        ? globalThis.String(object.accessToken)
        : isSet(object.access_token)
          ? globalThis.String(object.access_token)
          : '',
      refreshToken: isSet(object.refreshToken)
        ? globalThis.String(object.refreshToken)
        : isSet(object.refresh_token)
          ? globalThis.String(object.refresh_token)
          : '',
      expiresUtc: isSet(object.expiresUtc)
        ? fromJsonTimestamp(object.expiresUtc)
        : isSet(object.expires_utc)
          ? fromJsonTimestamp(object.expires_utc)
          : undefined,
    };
  },

  toJSON(message: ExchangeTokenResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    if (message.accountId !== '') {
      obj.accountId = message.accountId;
    }
    if (message.accessToken !== '') {
      obj.accessToken = message.accessToken;
    }
    if (message.refreshToken !== '') {
      obj.refreshToken = message.refreshToken;
    }
    if (message.expiresUtc !== undefined) {
      obj.expiresUtc = message.expiresUtc.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExchangeTokenResponse>, I>>(base?: I): ExchangeTokenResponse {
    return ExchangeTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExchangeTokenResponse>, I>>(object: I): ExchangeTokenResponse {
    const message = createBaseExchangeTokenResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.userId = object.userId ?? '';
    message.accountId = object.accountId ?? '';
    message.accessToken = object.accessToken ?? '';
    message.refreshToken = object.refreshToken ?? '';
    message.expiresUtc = object.expiresUtc ?? undefined;
    return message;
  },
};

function createBaseGetActiveAccountRequest(): GetActiveAccountRequest {
  return {};
}

export const GetActiveAccountRequest: MessageFns<GetActiveAccountRequest> = {
  encode(_: GetActiveAccountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetActiveAccountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetActiveAccountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetActiveAccountRequest {
    return {};
  },

  toJSON(_: GetActiveAccountRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetActiveAccountRequest>, I>>(base?: I): GetActiveAccountRequest {
    return GetActiveAccountRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetActiveAccountRequest>, I>>(_: I): GetActiveAccountRequest {
    const message = createBaseGetActiveAccountRequest();
    return message;
  },
};

function createBaseGetActiveAccountResponse(): GetActiveAccountResponse {
  return { responseDetails: undefined, result: undefined };
}

export const GetActiveAccountResponse: MessageFns<GetActiveAccountResponse> = {
  encode(message: GetActiveAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.result !== undefined) {
      Account.encode(message.result, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetActiveAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetActiveAccountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.result = Account.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetActiveAccountResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      result: isSet(object.result) ? Account.fromJSON(object.result) : undefined,
    };
  },

  toJSON(message: GetActiveAccountResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.result !== undefined) {
      obj.result = Account.toJSON(message.result);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetActiveAccountResponse>, I>>(base?: I): GetActiveAccountResponse {
    return GetActiveAccountResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetActiveAccountResponse>, I>>(object: I): GetActiveAccountResponse {
    const message = createBaseGetActiveAccountResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.result =
      object.result !== undefined && object.result !== null ? Account.fromPartial(object.result) : undefined;
    return message;
  },
};

function createBaseGetAuthStatusRequest(): GetAuthStatusRequest {
  return {};
}

export const GetAuthStatusRequest: MessageFns<GetAuthStatusRequest> = {
  encode(_: GetAuthStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAuthStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAuthStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetAuthStatusRequest {
    return {};
  },

  toJSON(_: GetAuthStatusRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAuthStatusRequest>, I>>(base?: I): GetAuthStatusRequest {
    return GetAuthStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAuthStatusRequest>, I>>(_: I): GetAuthStatusRequest {
    const message = createBaseGetAuthStatusRequest();
    return message;
  },
};

function createBaseGetAuthStatusResponse(): GetAuthStatusResponse {
  return {
    responseDetails: undefined,
    userId: '',
    accountStatus: '',
    accountStatusExplanation: '',
    activeAccount: '',
    requiresPasswordChange: false,
  };
}

export const GetAuthStatusResponse: MessageFns<GetAuthStatusResponse> = {
  encode(message: GetAuthStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.userId !== '') {
      writer.uint32(18).string(message.userId);
    }
    if (message.accountStatus !== '') {
      writer.uint32(26).string(message.accountStatus);
    }
    if (message.accountStatusExplanation !== '') {
      writer.uint32(34).string(message.accountStatusExplanation);
    }
    if (message.activeAccount !== '') {
      writer.uint32(42).string(message.activeAccount);
    }
    if (message.requiresPasswordChange !== false) {
      writer.uint32(48).bool(message.requiresPasswordChange);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAuthStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAuthStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.accountStatus = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.accountStatusExplanation = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.activeAccount = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.requiresPasswordChange = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAuthStatusResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
      accountStatus: isSet(object.accountStatus)
        ? globalThis.String(object.accountStatus)
        : isSet(object.account_status)
          ? globalThis.String(object.account_status)
          : '',
      accountStatusExplanation: isSet(object.accountStatusExplanation)
        ? globalThis.String(object.accountStatusExplanation)
        : isSet(object.account_status_explanation)
          ? globalThis.String(object.account_status_explanation)
          : '',
      activeAccount: isSet(object.activeAccount)
        ? globalThis.String(object.activeAccount)
        : isSet(object.active_account)
          ? globalThis.String(object.active_account)
          : '',
      requiresPasswordChange: isSet(object.requiresPasswordChange)
        ? globalThis.Boolean(object.requiresPasswordChange)
        : isSet(object.requires_password_change)
          ? globalThis.Boolean(object.requires_password_change)
          : false,
    };
  },

  toJSON(message: GetAuthStatusResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    if (message.accountStatus !== '') {
      obj.accountStatus = message.accountStatus;
    }
    if (message.accountStatusExplanation !== '') {
      obj.accountStatusExplanation = message.accountStatusExplanation;
    }
    if (message.activeAccount !== '') {
      obj.activeAccount = message.activeAccount;
    }
    if (message.requiresPasswordChange !== false) {
      obj.requiresPasswordChange = message.requiresPasswordChange;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAuthStatusResponse>, I>>(base?: I): GetAuthStatusResponse {
    return GetAuthStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAuthStatusResponse>, I>>(object: I): GetAuthStatusResponse {
    const message = createBaseGetAuthStatusResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.userId = object.userId ?? '';
    message.accountStatus = object.accountStatus ?? '';
    message.accountStatusExplanation = object.accountStatusExplanation ?? '';
    message.activeAccount = object.activeAccount ?? '';
    message.requiresPasswordChange = object.requiresPasswordChange ?? false;
    return message;
  },
};

function createBaseRedeemPasswordResetTokenRequest(): RedeemPasswordResetTokenRequest {
  return { token: '', newPassword: '' };
}

export const RedeemPasswordResetTokenRequest: MessageFns<RedeemPasswordResetTokenRequest> = {
  encode(message: RedeemPasswordResetTokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== '') {
      writer.uint32(10).string(message.token);
    }
    if (message.newPassword !== '') {
      writer.uint32(18).string(message.newPassword);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RedeemPasswordResetTokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRedeemPasswordResetTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newPassword = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RedeemPasswordResetTokenRequest {
    return {
      token: isSet(object.token) ? globalThis.String(object.token) : '',
      newPassword: isSet(object.newPassword)
        ? globalThis.String(object.newPassword)
        : isSet(object.new_password)
          ? globalThis.String(object.new_password)
          : '',
    };
  },

  toJSON(message: RedeemPasswordResetTokenRequest): unknown {
    const obj: any = {};
    if (message.token !== '') {
      obj.token = message.token;
    }
    if (message.newPassword !== '') {
      obj.newPassword = message.newPassword;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RedeemPasswordResetTokenRequest>, I>>(base?: I): RedeemPasswordResetTokenRequest {
    return RedeemPasswordResetTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RedeemPasswordResetTokenRequest>, I>>(
    object: I,
  ): RedeemPasswordResetTokenRequest {
    const message = createBaseRedeemPasswordResetTokenRequest();
    message.token = object.token ?? '';
    message.newPassword = object.newPassword ?? '';
    return message;
  },
};

function createBaseRedeemPasswordResetTokenResponse(): RedeemPasswordResetTokenResponse {
  return { responseDetails: undefined };
}

export const RedeemPasswordResetTokenResponse: MessageFns<RedeemPasswordResetTokenResponse> = {
  encode(message: RedeemPasswordResetTokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RedeemPasswordResetTokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRedeemPasswordResetTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RedeemPasswordResetTokenResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
    };
  },

  toJSON(message: RedeemPasswordResetTokenResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RedeemPasswordResetTokenResponse>, I>>(
    base?: I,
  ): RedeemPasswordResetTokenResponse {
    return RedeemPasswordResetTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RedeemPasswordResetTokenResponse>, I>>(
    object: I,
  ): RedeemPasswordResetTokenResponse {
    const message = createBaseRedeemPasswordResetTokenResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    return message;
  },
};

function createBaseRefreshTOTPSecretRequest(): RefreshTOTPSecretRequest {
  return { currentPassword: '', totpToken: '' };
}

export const RefreshTOTPSecretRequest: MessageFns<RefreshTOTPSecretRequest> = {
  encode(message: RefreshTOTPSecretRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.currentPassword !== '') {
      writer.uint32(10).string(message.currentPassword);
    }
    if (message.totpToken !== '') {
      writer.uint32(18).string(message.totpToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefreshTOTPSecretRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshTOTPSecretRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.currentPassword = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.totpToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefreshTOTPSecretRequest {
    return {
      currentPassword: isSet(object.currentPassword)
        ? globalThis.String(object.currentPassword)
        : isSet(object.current_password)
          ? globalThis.String(object.current_password)
          : '',
      totpToken: isSet(object.totpToken)
        ? globalThis.String(object.totpToken)
        : isSet(object.totp_token)
          ? globalThis.String(object.totp_token)
          : '',
    };
  },

  toJSON(message: RefreshTOTPSecretRequest): unknown {
    const obj: any = {};
    if (message.currentPassword !== '') {
      obj.currentPassword = message.currentPassword;
    }
    if (message.totpToken !== '') {
      obj.totpToken = message.totpToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RefreshTOTPSecretRequest>, I>>(base?: I): RefreshTOTPSecretRequest {
    return RefreshTOTPSecretRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RefreshTOTPSecretRequest>, I>>(object: I): RefreshTOTPSecretRequest {
    const message = createBaseRefreshTOTPSecretRequest();
    message.currentPassword = object.currentPassword ?? '';
    message.totpToken = object.totpToken ?? '';
    return message;
  },
};

function createBaseRefreshTOTPSecretResponse(): RefreshTOTPSecretResponse {
  return { responseDetails: undefined, result: undefined };
}

export const RefreshTOTPSecretResponse: MessageFns<RefreshTOTPSecretResponse> = {
  encode(message: RefreshTOTPSecretResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.result !== undefined) {
      TOTPSecretRefreshResponse.encode(message.result, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefreshTOTPSecretResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshTOTPSecretResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.result = TOTPSecretRefreshResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefreshTOTPSecretResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      result: isSet(object.result) ? TOTPSecretRefreshResponse.fromJSON(object.result) : undefined,
    };
  },

  toJSON(message: RefreshTOTPSecretResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.result !== undefined) {
      obj.result = TOTPSecretRefreshResponse.toJSON(message.result);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RefreshTOTPSecretResponse>, I>>(base?: I): RefreshTOTPSecretResponse {
    return RefreshTOTPSecretResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RefreshTOTPSecretResponse>, I>>(object: I): RefreshTOTPSecretResponse {
    const message = createBaseRefreshTOTPSecretResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.result =
      object.result !== undefined && object.result !== null
        ? TOTPSecretRefreshResponse.fromPartial(object.result)
        : undefined;
    return message;
  },
};

function createBaseRequestEmailVerificationEmailRequest(): RequestEmailVerificationEmailRequest {
  return { emailAddress: '' };
}

export const RequestEmailVerificationEmailRequest: MessageFns<RequestEmailVerificationEmailRequest> = {
  encode(message: RequestEmailVerificationEmailRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.emailAddress !== '') {
      writer.uint32(10).string(message.emailAddress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestEmailVerificationEmailRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestEmailVerificationEmailRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.emailAddress = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestEmailVerificationEmailRequest {
    return {
      emailAddress: isSet(object.emailAddress)
        ? globalThis.String(object.emailAddress)
        : isSet(object.email_address)
          ? globalThis.String(object.email_address)
          : '',
    };
  },

  toJSON(message: RequestEmailVerificationEmailRequest): unknown {
    const obj: any = {};
    if (message.emailAddress !== '') {
      obj.emailAddress = message.emailAddress;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestEmailVerificationEmailRequest>, I>>(
    base?: I,
  ): RequestEmailVerificationEmailRequest {
    return RequestEmailVerificationEmailRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestEmailVerificationEmailRequest>, I>>(
    object: I,
  ): RequestEmailVerificationEmailRequest {
    const message = createBaseRequestEmailVerificationEmailRequest();
    message.emailAddress = object.emailAddress ?? '';
    return message;
  },
};

function createBaseRequestEmailVerificationEmailResponse(): RequestEmailVerificationEmailResponse {
  return { responseDetails: undefined, submitted: false };
}

export const RequestEmailVerificationEmailResponse: MessageFns<RequestEmailVerificationEmailResponse> = {
  encode(message: RequestEmailVerificationEmailResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.submitted !== false) {
      writer.uint32(16).bool(message.submitted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestEmailVerificationEmailResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestEmailVerificationEmailResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.submitted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestEmailVerificationEmailResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      submitted: isSet(object.submitted) ? globalThis.Boolean(object.submitted) : false,
    };
  },

  toJSON(message: RequestEmailVerificationEmailResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.submitted !== false) {
      obj.submitted = message.submitted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestEmailVerificationEmailResponse>, I>>(
    base?: I,
  ): RequestEmailVerificationEmailResponse {
    return RequestEmailVerificationEmailResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestEmailVerificationEmailResponse>, I>>(
    object: I,
  ): RequestEmailVerificationEmailResponse {
    const message = createBaseRequestEmailVerificationEmailResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.submitted = object.submitted ?? false;
    return message;
  },
};

function createBaseRequestPasswordResetTokenRequest(): RequestPasswordResetTokenRequest {
  return { emailAddress: '' };
}

export const RequestPasswordResetTokenRequest: MessageFns<RequestPasswordResetTokenRequest> = {
  encode(message: RequestPasswordResetTokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.emailAddress !== '') {
      writer.uint32(10).string(message.emailAddress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestPasswordResetTokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestPasswordResetTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.emailAddress = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestPasswordResetTokenRequest {
    return {
      emailAddress: isSet(object.emailAddress)
        ? globalThis.String(object.emailAddress)
        : isSet(object.email_address)
          ? globalThis.String(object.email_address)
          : '',
    };
  },

  toJSON(message: RequestPasswordResetTokenRequest): unknown {
    const obj: any = {};
    if (message.emailAddress !== '') {
      obj.emailAddress = message.emailAddress;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestPasswordResetTokenRequest>, I>>(
    base?: I,
  ): RequestPasswordResetTokenRequest {
    return RequestPasswordResetTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestPasswordResetTokenRequest>, I>>(
    object: I,
  ): RequestPasswordResetTokenRequest {
    const message = createBaseRequestPasswordResetTokenRequest();
    message.emailAddress = object.emailAddress ?? '';
    return message;
  },
};

function createBaseRequestPasswordResetTokenResponse(): RequestPasswordResetTokenResponse {
  return { responseDetails: undefined, accepted: false };
}

export const RequestPasswordResetTokenResponse: MessageFns<RequestPasswordResetTokenResponse> = {
  encode(message: RequestPasswordResetTokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.accepted !== false) {
      writer.uint32(16).bool(message.accepted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestPasswordResetTokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestPasswordResetTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestPasswordResetTokenResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
    };
  },

  toJSON(message: RequestPasswordResetTokenResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestPasswordResetTokenResponse>, I>>(
    base?: I,
  ): RequestPasswordResetTokenResponse {
    return RequestPasswordResetTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestPasswordResetTokenResponse>, I>>(
    object: I,
  ): RequestPasswordResetTokenResponse {
    const message = createBaseRequestPasswordResetTokenResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.accepted = object.accepted ?? false;
    return message;
  },
};

function createBaseRequestUsernameReminderRequest(): RequestUsernameReminderRequest {
  return { emailAddress: '' };
}

export const RequestUsernameReminderRequest: MessageFns<RequestUsernameReminderRequest> = {
  encode(message: RequestUsernameReminderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.emailAddress !== '') {
      writer.uint32(10).string(message.emailAddress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestUsernameReminderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestUsernameReminderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.emailAddress = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestUsernameReminderRequest {
    return {
      emailAddress: isSet(object.emailAddress)
        ? globalThis.String(object.emailAddress)
        : isSet(object.email_address)
          ? globalThis.String(object.email_address)
          : '',
    };
  },

  toJSON(message: RequestUsernameReminderRequest): unknown {
    const obj: any = {};
    if (message.emailAddress !== '') {
      obj.emailAddress = message.emailAddress;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestUsernameReminderRequest>, I>>(base?: I): RequestUsernameReminderRequest {
    return RequestUsernameReminderRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestUsernameReminderRequest>, I>>(
    object: I,
  ): RequestUsernameReminderRequest {
    const message = createBaseRequestUsernameReminderRequest();
    message.emailAddress = object.emailAddress ?? '';
    return message;
  },
};

function createBaseRequestUsernameReminderResponse(): RequestUsernameReminderResponse {
  return { responseDetails: undefined, submitted: false };
}

export const RequestUsernameReminderResponse: MessageFns<RequestUsernameReminderResponse> = {
  encode(message: RequestUsernameReminderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.submitted !== false) {
      writer.uint32(16).bool(message.submitted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestUsernameReminderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestUsernameReminderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.submitted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestUsernameReminderResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      submitted: isSet(object.submitted) ? globalThis.Boolean(object.submitted) : false,
    };
  },

  toJSON(message: RequestUsernameReminderResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.submitted !== false) {
      obj.submitted = message.submitted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestUsernameReminderResponse>, I>>(base?: I): RequestUsernameReminderResponse {
    return RequestUsernameReminderResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestUsernameReminderResponse>, I>>(
    object: I,
  ): RequestUsernameReminderResponse {
    const message = createBaseRequestUsernameReminderResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.submitted = object.submitted ?? false;
    return message;
  },
};

function createBaseUpdatePasswordRequest(): UpdatePasswordRequest {
  return { newPassword: '', currentPassword: '', totpToken: '' };
}

export const UpdatePasswordRequest: MessageFns<UpdatePasswordRequest> = {
  encode(message: UpdatePasswordRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.newPassword !== '') {
      writer.uint32(10).string(message.newPassword);
    }
    if (message.currentPassword !== '') {
      writer.uint32(18).string(message.currentPassword);
    }
    if (message.totpToken !== '') {
      writer.uint32(26).string(message.totpToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdatePasswordRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdatePasswordRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.newPassword = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.currentPassword = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.totpToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdatePasswordRequest {
    return {
      newPassword: isSet(object.newPassword)
        ? globalThis.String(object.newPassword)
        : isSet(object.new_password)
          ? globalThis.String(object.new_password)
          : '',
      currentPassword: isSet(object.currentPassword)
        ? globalThis.String(object.currentPassword)
        : isSet(object.current_password)
          ? globalThis.String(object.current_password)
          : '',
      totpToken: isSet(object.totpToken)
        ? globalThis.String(object.totpToken)
        : isSet(object.totp_token)
          ? globalThis.String(object.totp_token)
          : '',
    };
  },

  toJSON(message: UpdatePasswordRequest): unknown {
    const obj: any = {};
    if (message.newPassword !== '') {
      obj.newPassword = message.newPassword;
    }
    if (message.currentPassword !== '') {
      obj.currentPassword = message.currentPassword;
    }
    if (message.totpToken !== '') {
      obj.totpToken = message.totpToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdatePasswordRequest>, I>>(base?: I): UpdatePasswordRequest {
    return UpdatePasswordRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdatePasswordRequest>, I>>(object: I): UpdatePasswordRequest {
    const message = createBaseUpdatePasswordRequest();
    message.newPassword = object.newPassword ?? '';
    message.currentPassword = object.currentPassword ?? '';
    message.totpToken = object.totpToken ?? '';
    return message;
  },
};

function createBaseUpdatePasswordResponse(): UpdatePasswordResponse {
  return { responseDetails: undefined };
}

export const UpdatePasswordResponse: MessageFns<UpdatePasswordResponse> = {
  encode(message: UpdatePasswordResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdatePasswordResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdatePasswordResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdatePasswordResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
    };
  },

  toJSON(message: UpdatePasswordResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdatePasswordResponse>, I>>(base?: I): UpdatePasswordResponse {
    return UpdatePasswordResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdatePasswordResponse>, I>>(object: I): UpdatePasswordResponse {
    const message = createBaseUpdatePasswordResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    return message;
  },
};

function createBaseVerifyEmailAddressRequest(): VerifyEmailAddressRequest {
  return { token: '' };
}

export const VerifyEmailAddressRequest: MessageFns<VerifyEmailAddressRequest> = {
  encode(message: VerifyEmailAddressRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== '') {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerifyEmailAddressRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifyEmailAddressRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifyEmailAddressRequest {
    return { token: isSet(object.token) ? globalThis.String(object.token) : '' };
  },

  toJSON(message: VerifyEmailAddressRequest): unknown {
    const obj: any = {};
    if (message.token !== '') {
      obj.token = message.token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VerifyEmailAddressRequest>, I>>(base?: I): VerifyEmailAddressRequest {
    return VerifyEmailAddressRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VerifyEmailAddressRequest>, I>>(object: I): VerifyEmailAddressRequest {
    const message = createBaseVerifyEmailAddressRequest();
    message.token = object.token ?? '';
    return message;
  },
};

function createBaseVerifyEmailAddressResponse(): VerifyEmailAddressResponse {
  return { responseDetails: undefined, verified: false };
}

export const VerifyEmailAddressResponse: MessageFns<VerifyEmailAddressResponse> = {
  encode(message: VerifyEmailAddressResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.verified !== false) {
      writer.uint32(16).bool(message.verified);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerifyEmailAddressResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifyEmailAddressResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.verified = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifyEmailAddressResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      verified: isSet(object.verified) ? globalThis.Boolean(object.verified) : false,
    };
  },

  toJSON(message: VerifyEmailAddressResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.verified !== false) {
      obj.verified = message.verified;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VerifyEmailAddressResponse>, I>>(base?: I): VerifyEmailAddressResponse {
    return VerifyEmailAddressResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VerifyEmailAddressResponse>, I>>(object: I): VerifyEmailAddressResponse {
    const message = createBaseVerifyEmailAddressResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.verified = object.verified ?? false;
    return message;
  },
};

function createBaseVerifyTOTPSecretRequest(): VerifyTOTPSecretRequest {
  return { totpToken: '', userId: '' };
}

export const VerifyTOTPSecretRequest: MessageFns<VerifyTOTPSecretRequest> = {
  encode(message: VerifyTOTPSecretRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totpToken !== '') {
      writer.uint32(10).string(message.totpToken);
    }
    if (message.userId !== '') {
      writer.uint32(18).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerifyTOTPSecretRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifyTOTPSecretRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.totpToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifyTOTPSecretRequest {
    return {
      totpToken: isSet(object.totpToken)
        ? globalThis.String(object.totpToken)
        : isSet(object.totp_token)
          ? globalThis.String(object.totp_token)
          : '',
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
    };
  },

  toJSON(message: VerifyTOTPSecretRequest): unknown {
    const obj: any = {};
    if (message.totpToken !== '') {
      obj.totpToken = message.totpToken;
    }
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VerifyTOTPSecretRequest>, I>>(base?: I): VerifyTOTPSecretRequest {
    return VerifyTOTPSecretRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VerifyTOTPSecretRequest>, I>>(object: I): VerifyTOTPSecretRequest {
    const message = createBaseVerifyTOTPSecretRequest();
    message.totpToken = object.totpToken ?? '';
    message.userId = object.userId ?? '';
    return message;
  },
};

function createBaseVerifyTOTPSecretResponse(): VerifyTOTPSecretResponse {
  return { responseDetails: undefined, accepted: false };
}

export const VerifyTOTPSecretResponse: MessageFns<VerifyTOTPSecretResponse> = {
  encode(message: VerifyTOTPSecretResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.accepted !== false) {
      writer.uint32(16).bool(message.accepted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerifyTOTPSecretResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifyTOTPSecretResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifyTOTPSecretResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
    };
  },

  toJSON(message: VerifyTOTPSecretResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VerifyTOTPSecretResponse>, I>>(base?: I): VerifyTOTPSecretResponse {
    return VerifyTOTPSecretResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VerifyTOTPSecretResponse>, I>>(object: I): VerifyTOTPSecretResponse {
    const message = createBaseVerifyTOTPSecretResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.accepted = object.accepted ?? false;
    return message;
  },
};

function createBaseEmailAddressVerificationRequest(): EmailAddressVerificationRequest {
  return { token: '' };
}

export const EmailAddressVerificationRequest: MessageFns<EmailAddressVerificationRequest> = {
  encode(message: EmailAddressVerificationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== '') {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmailAddressVerificationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmailAddressVerificationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EmailAddressVerificationRequest {
    return { token: isSet(object.token) ? globalThis.String(object.token) : '' };
  },

  toJSON(message: EmailAddressVerificationRequest): unknown {
    const obj: any = {};
    if (message.token !== '') {
      obj.token = message.token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EmailAddressVerificationRequest>, I>>(base?: I): EmailAddressVerificationRequest {
    return EmailAddressVerificationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EmailAddressVerificationRequest>, I>>(
    object: I,
  ): EmailAddressVerificationRequest {
    const message = createBaseEmailAddressVerificationRequest();
    message.token = object.token ?? '';
    return message;
  },
};

function createBaseEmailAddressVerificationResponse(): EmailAddressVerificationResponse {
  return { responseDetails: undefined };
}

export const EmailAddressVerificationResponse: MessageFns<EmailAddressVerificationResponse> = {
  encode(message: EmailAddressVerificationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmailAddressVerificationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmailAddressVerificationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EmailAddressVerificationResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
    };
  },

  toJSON(message: EmailAddressVerificationResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EmailAddressVerificationResponse>, I>>(
    base?: I,
  ): EmailAddressVerificationResponse {
    return EmailAddressVerificationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EmailAddressVerificationResponse>, I>>(
    object: I,
  ): EmailAddressVerificationResponse {
    const message = createBaseEmailAddressVerificationResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    return message;
  },
};

function createBasePasswordResetResponse(): PasswordResetResponse {
  return { responseDetails: undefined, successful: false };
}

export const PasswordResetResponse: MessageFns<PasswordResetResponse> = {
  encode(message: PasswordResetResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.successful !== false) {
      writer.uint32(16).bool(message.successful);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PasswordResetResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePasswordResetResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.successful = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PasswordResetResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      successful: isSet(object.successful) ? globalThis.Boolean(object.successful) : false,
    };
  },

  toJSON(message: PasswordResetResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.successful !== false) {
      obj.successful = message.successful;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PasswordResetResponse>, I>>(base?: I): PasswordResetResponse {
    return PasswordResetResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PasswordResetResponse>, I>>(object: I): PasswordResetResponse {
    const message = createBasePasswordResetResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.successful = object.successful ?? false;
    return message;
  },
};

function createBasePasswordResetTokenCreationRequestInput(): PasswordResetTokenCreationRequestInput {
  return { emailAddress: '' };
}

export const PasswordResetTokenCreationRequestInput: MessageFns<PasswordResetTokenCreationRequestInput> = {
  encode(message: PasswordResetTokenCreationRequestInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.emailAddress !== '') {
      writer.uint32(10).string(message.emailAddress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PasswordResetTokenCreationRequestInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePasswordResetTokenCreationRequestInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.emailAddress = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PasswordResetTokenCreationRequestInput {
    return {
      emailAddress: isSet(object.emailAddress)
        ? globalThis.String(object.emailAddress)
        : isSet(object.email_address)
          ? globalThis.String(object.email_address)
          : '',
    };
  },

  toJSON(message: PasswordResetTokenCreationRequestInput): unknown {
    const obj: any = {};
    if (message.emailAddress !== '') {
      obj.emailAddress = message.emailAddress;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PasswordResetTokenCreationRequestInput>, I>>(
    base?: I,
  ): PasswordResetTokenCreationRequestInput {
    return PasswordResetTokenCreationRequestInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PasswordResetTokenCreationRequestInput>, I>>(
    object: I,
  ): PasswordResetTokenCreationRequestInput {
    const message = createBasePasswordResetTokenCreationRequestInput();
    message.emailAddress = object.emailAddress ?? '';
    return message;
  },
};

function createBasePasswordResetTokenRedemptionRequestInput(): PasswordResetTokenRedemptionRequestInput {
  return { token: '', newPassword: '' };
}

export const PasswordResetTokenRedemptionRequestInput: MessageFns<PasswordResetTokenRedemptionRequestInput> = {
  encode(message: PasswordResetTokenRedemptionRequestInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== '') {
      writer.uint32(10).string(message.token);
    }
    if (message.newPassword !== '') {
      writer.uint32(18).string(message.newPassword);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PasswordResetTokenRedemptionRequestInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePasswordResetTokenRedemptionRequestInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newPassword = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PasswordResetTokenRedemptionRequestInput {
    return {
      token: isSet(object.token) ? globalThis.String(object.token) : '',
      newPassword: isSet(object.newPassword)
        ? globalThis.String(object.newPassword)
        : isSet(object.new_password)
          ? globalThis.String(object.new_password)
          : '',
    };
  },

  toJSON(message: PasswordResetTokenRedemptionRequestInput): unknown {
    const obj: any = {};
    if (message.token !== '') {
      obj.token = message.token;
    }
    if (message.newPassword !== '') {
      obj.newPassword = message.newPassword;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PasswordResetTokenRedemptionRequestInput>, I>>(
    base?: I,
  ): PasswordResetTokenRedemptionRequestInput {
    return PasswordResetTokenRedemptionRequestInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PasswordResetTokenRedemptionRequestInput>, I>>(
    object: I,
  ): PasswordResetTokenRedemptionRequestInput {
    const message = createBasePasswordResetTokenRedemptionRequestInput();
    message.token = object.token ?? '';
    message.newPassword = object.newPassword ?? '';
    return message;
  },
};

function createBaseTOTPSecretRefreshResponse(): TOTPSecretRefreshResponse {
  return { twoFactorQrCode: '', twoFactorSecret: '' };
}

export const TOTPSecretRefreshResponse: MessageFns<TOTPSecretRefreshResponse> = {
  encode(message: TOTPSecretRefreshResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.twoFactorQrCode !== '') {
      writer.uint32(18).string(message.twoFactorQrCode);
    }
    if (message.twoFactorSecret !== '') {
      writer.uint32(26).string(message.twoFactorSecret);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TOTPSecretRefreshResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTOTPSecretRefreshResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.twoFactorQrCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.twoFactorSecret = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TOTPSecretRefreshResponse {
    return {
      twoFactorQrCode: isSet(object.twoFactorQrCode)
        ? globalThis.String(object.twoFactorQrCode)
        : isSet(object.two_factor_qr_code)
          ? globalThis.String(object.two_factor_qr_code)
          : '',
      twoFactorSecret: isSet(object.twoFactorSecret)
        ? globalThis.String(object.twoFactorSecret)
        : isSet(object.two_factor_secret)
          ? globalThis.String(object.two_factor_secret)
          : '',
    };
  },

  toJSON(message: TOTPSecretRefreshResponse): unknown {
    const obj: any = {};
    if (message.twoFactorQrCode !== '') {
      obj.twoFactorQrCode = message.twoFactorQrCode;
    }
    if (message.twoFactorSecret !== '') {
      obj.twoFactorSecret = message.twoFactorSecret;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TOTPSecretRefreshResponse>, I>>(base?: I): TOTPSecretRefreshResponse {
    return TOTPSecretRefreshResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TOTPSecretRefreshResponse>, I>>(object: I): TOTPSecretRefreshResponse {
    const message = createBaseTOTPSecretRefreshResponse();
    message.twoFactorQrCode = object.twoFactorQrCode ?? '';
    message.twoFactorSecret = object.twoFactorSecret ?? '';
    return message;
  },
};

function createBaseTOTPSecretVerificationResponse(): TOTPSecretVerificationResponse {
  return { responseDetails: undefined, accepted: false };
}

export const TOTPSecretVerificationResponse: MessageFns<TOTPSecretVerificationResponse> = {
  encode(message: TOTPSecretVerificationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.accepted !== false) {
      writer.uint32(16).bool(message.accepted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TOTPSecretVerificationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTOTPSecretVerificationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TOTPSecretVerificationResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
    };
  },

  toJSON(message: TOTPSecretVerificationResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TOTPSecretVerificationResponse>, I>>(base?: I): TOTPSecretVerificationResponse {
    return TOTPSecretVerificationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TOTPSecretVerificationResponse>, I>>(
    object: I,
  ): TOTPSecretVerificationResponse {
    const message = createBaseTOTPSecretVerificationResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.accepted = object.accepted ?? false;
    return message;
  },
};

function createBaseTokenResponse(): TokenResponse {
  return { userId: '', accountId: '', accessToken: '', refreshToken: '', expiresUtc: undefined };
}

export const TokenResponse: MessageFns<TokenResponse> = {
  encode(message: TokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== '') {
      writer.uint32(18).string(message.userId);
    }
    if (message.accountId !== '') {
      writer.uint32(26).string(message.accountId);
    }
    if (message.accessToken !== '') {
      writer.uint32(34).string(message.accessToken);
    }
    if (message.refreshToken !== '') {
      writer.uint32(42).string(message.refreshToken);
    }
    if (message.expiresUtc !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresUtc), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.accessToken = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.refreshToken = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.expiresUtc = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TokenResponse {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
      accountId: isSet(object.accountId)
        ? globalThis.String(object.accountId)
        : isSet(object.account_id)
          ? globalThis.String(object.account_id)
          : '',
      accessToken: isSet(object.accessToken)
        ? globalThis.String(object.accessToken)
        : isSet(object.access_token)
          ? globalThis.String(object.access_token)
          : '',
      refreshToken: isSet(object.refreshToken)
        ? globalThis.String(object.refreshToken)
        : isSet(object.refresh_token)
          ? globalThis.String(object.refresh_token)
          : '',
      expiresUtc: isSet(object.expiresUtc)
        ? fromJsonTimestamp(object.expiresUtc)
        : isSet(object.expires_utc)
          ? fromJsonTimestamp(object.expires_utc)
          : undefined,
    };
  },

  toJSON(message: TokenResponse): unknown {
    const obj: any = {};
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    if (message.accountId !== '') {
      obj.accountId = message.accountId;
    }
    if (message.accessToken !== '') {
      obj.accessToken = message.accessToken;
    }
    if (message.refreshToken !== '') {
      obj.refreshToken = message.refreshToken;
    }
    if (message.expiresUtc !== undefined) {
      obj.expiresUtc = message.expiresUtc.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TokenResponse>, I>>(base?: I): TokenResponse {
    return TokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TokenResponse>, I>>(object: I): TokenResponse {
    const message = createBaseTokenResponse();
    message.userId = object.userId ?? '';
    message.accountId = object.accountId ?? '';
    message.accessToken = object.accessToken ?? '';
    message.refreshToken = object.refreshToken ?? '';
    message.expiresUtc = object.expiresUtc ?? undefined;
    return message;
  },
};

function createBaseUserPermissionsRequestInput(): UserPermissionsRequestInput {
  return { permissions: [] };
}

export const UserPermissionsRequestInput: MessageFns<UserPermissionsRequestInput> = {
  encode(message: UserPermissionsRequestInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.permissions) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserPermissionsRequestInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserPermissionsRequestInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.permissions.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserPermissionsRequestInput {
    return {
      permissions: globalThis.Array.isArray(object?.permissions)
        ? object.permissions.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: UserPermissionsRequestInput): unknown {
    const obj: any = {};
    if (message.permissions?.length) {
      obj.permissions = message.permissions;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserPermissionsRequestInput>, I>>(base?: I): UserPermissionsRequestInput {
    return UserPermissionsRequestInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserPermissionsRequestInput>, I>>(object: I): UserPermissionsRequestInput {
    const message = createBaseUserPermissionsRequestInput();
    message.permissions = object.permissions?.map((e) => e) || [];
    return message;
  },
};

function createBaseUserPermissionsResponse(): UserPermissionsResponse {
  return { responseDetails: undefined, permissions: {} };
}

export const UserPermissionsResponse: MessageFns<UserPermissionsResponse> = {
  encode(message: UserPermissionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    globalThis.Object.entries(message.permissions).forEach(([key, value]: [string, boolean]) => {
      UserPermissionsResponse_PermissionsEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserPermissionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserPermissionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = UserPermissionsResponse_PermissionsEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.permissions[entry2.key] = entry2.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserPermissionsResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      permissions: isObject(object.permissions)
        ? (globalThis.Object.entries(object.permissions) as [string, any][]).reduce(
            (acc: { [key: string]: boolean }, [key, value]: [string, any]) => {
              acc[key] = globalThis.Boolean(value);
              return acc;
            },
            {},
          )
        : {},
    };
  },

  toJSON(message: UserPermissionsResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.permissions) {
      const entries = globalThis.Object.entries(message.permissions) as [string, boolean][];
      if (entries.length > 0) {
        obj.permissions = {};
        entries.forEach(([k, v]) => {
          obj.permissions[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserPermissionsResponse>, I>>(base?: I): UserPermissionsResponse {
    return UserPermissionsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserPermissionsResponse>, I>>(object: I): UserPermissionsResponse {
    const message = createBaseUserPermissionsResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.permissions = (globalThis.Object.entries(object.permissions ?? {}) as [string, boolean][]).reduce(
      (acc: { [key: string]: boolean }, [key, value]: [string, boolean]) => {
        if (value !== undefined) {
          acc[key] = globalThis.Boolean(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseUserPermissionsResponse_PermissionsEntry(): UserPermissionsResponse_PermissionsEntry {
  return { key: '', value: false };
}

export const UserPermissionsResponse_PermissionsEntry: MessageFns<UserPermissionsResponse_PermissionsEntry> = {
  encode(message: UserPermissionsResponse_PermissionsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== '') {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== false) {
      writer.uint32(16).bool(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserPermissionsResponse_PermissionsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserPermissionsResponse_PermissionsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserPermissionsResponse_PermissionsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : '',
      value: isSet(object.value) ? globalThis.Boolean(object.value) : false,
    };
  },

  toJSON(message: UserPermissionsResponse_PermissionsEntry): unknown {
    const obj: any = {};
    if (message.key !== '') {
      obj.key = message.key;
    }
    if (message.value !== false) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserPermissionsResponse_PermissionsEntry>, I>>(
    base?: I,
  ): UserPermissionsResponse_PermissionsEntry {
    return UserPermissionsResponse_PermissionsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserPermissionsResponse_PermissionsEntry>, I>>(
    object: I,
  ): UserPermissionsResponse_PermissionsEntry {
    const message = createBaseUserPermissionsResponse_PermissionsEntry();
    message.key = object.key ?? '';
    message.value = object.value ?? false;
    return message;
  },
};

function createBaseUsernameReminderRequestInput(): UsernameReminderRequestInput {
  return { emailAddress: '' };
}

export const UsernameReminderRequestInput: MessageFns<UsernameReminderRequestInput> = {
  encode(message: UsernameReminderRequestInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.emailAddress !== '') {
      writer.uint32(10).string(message.emailAddress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UsernameReminderRequestInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUsernameReminderRequestInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.emailAddress = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UsernameReminderRequestInput {
    return {
      emailAddress: isSet(object.emailAddress)
        ? globalThis.String(object.emailAddress)
        : isSet(object.email_address)
          ? globalThis.String(object.email_address)
          : '',
    };
  },

  toJSON(message: UsernameReminderRequestInput): unknown {
    const obj: any = {};
    if (message.emailAddress !== '') {
      obj.emailAddress = message.emailAddress;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UsernameReminderRequestInput>, I>>(base?: I): UsernameReminderRequestInput {
    return UsernameReminderRequestInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UsernameReminderRequestInput>, I>>(object: I): UsernameReminderRequestInput {
    const message = createBaseUsernameReminderRequestInput();
    message.emailAddress = object.emailAddress ?? '';
    return message;
  },
};

function createBaseEvaluateBooleanFeatureFlagRequest(): EvaluateBooleanFeatureFlagRequest {
  return { featureFlag: '' };
}

export const EvaluateBooleanFeatureFlagRequest: MessageFns<EvaluateBooleanFeatureFlagRequest> = {
  encode(message: EvaluateBooleanFeatureFlagRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.featureFlag !== '') {
      writer.uint32(10).string(message.featureFlag);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EvaluateBooleanFeatureFlagRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvaluateBooleanFeatureFlagRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.featureFlag = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EvaluateBooleanFeatureFlagRequest {
    return {
      featureFlag: isSet(object.featureFlag)
        ? globalThis.String(object.featureFlag)
        : isSet(object.feature_flag)
          ? globalThis.String(object.feature_flag)
          : '',
    };
  },

  toJSON(message: EvaluateBooleanFeatureFlagRequest): unknown {
    const obj: any = {};
    if (message.featureFlag !== '') {
      obj.featureFlag = message.featureFlag;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EvaluateBooleanFeatureFlagRequest>, I>>(
    base?: I,
  ): EvaluateBooleanFeatureFlagRequest {
    return EvaluateBooleanFeatureFlagRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EvaluateBooleanFeatureFlagRequest>, I>>(
    object: I,
  ): EvaluateBooleanFeatureFlagRequest {
    const message = createBaseEvaluateBooleanFeatureFlagRequest();
    message.featureFlag = object.featureFlag ?? '';
    return message;
  },
};

function createBaseEvaluateBooleanFeatureFlagResponse(): EvaluateBooleanFeatureFlagResponse {
  return { responseDetails: undefined, enabled: false };
}

export const EvaluateBooleanFeatureFlagResponse: MessageFns<EvaluateBooleanFeatureFlagResponse> = {
  encode(message: EvaluateBooleanFeatureFlagResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.enabled !== false) {
      writer.uint32(16).bool(message.enabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EvaluateBooleanFeatureFlagResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvaluateBooleanFeatureFlagResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EvaluateBooleanFeatureFlagResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
    };
  },

  toJSON(message: EvaluateBooleanFeatureFlagResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EvaluateBooleanFeatureFlagResponse>, I>>(
    base?: I,
  ): EvaluateBooleanFeatureFlagResponse {
    return EvaluateBooleanFeatureFlagResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EvaluateBooleanFeatureFlagResponse>, I>>(
    object: I,
  ): EvaluateBooleanFeatureFlagResponse {
    const message = createBaseEvaluateBooleanFeatureFlagResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.enabled = object.enabled ?? false;
    return message;
  },
};

function createBaseEvaluateInt64FeatureFlagRequest(): EvaluateInt64FeatureFlagRequest {
  return { featureFlag: '' };
}

export const EvaluateInt64FeatureFlagRequest: MessageFns<EvaluateInt64FeatureFlagRequest> = {
  encode(message: EvaluateInt64FeatureFlagRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.featureFlag !== '') {
      writer.uint32(10).string(message.featureFlag);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EvaluateInt64FeatureFlagRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvaluateInt64FeatureFlagRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.featureFlag = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EvaluateInt64FeatureFlagRequest {
    return {
      featureFlag: isSet(object.featureFlag)
        ? globalThis.String(object.featureFlag)
        : isSet(object.feature_flag)
          ? globalThis.String(object.feature_flag)
          : '',
    };
  },

  toJSON(message: EvaluateInt64FeatureFlagRequest): unknown {
    const obj: any = {};
    if (message.featureFlag !== '') {
      obj.featureFlag = message.featureFlag;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EvaluateInt64FeatureFlagRequest>, I>>(base?: I): EvaluateInt64FeatureFlagRequest {
    return EvaluateInt64FeatureFlagRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EvaluateInt64FeatureFlagRequest>, I>>(
    object: I,
  ): EvaluateInt64FeatureFlagRequest {
    const message = createBaseEvaluateInt64FeatureFlagRequest();
    message.featureFlag = object.featureFlag ?? '';
    return message;
  },
};

function createBaseEvaluateInt64FeatureFlagResponse(): EvaluateInt64FeatureFlagResponse {
  return { responseDetails: undefined, value: 0 };
}

export const EvaluateInt64FeatureFlagResponse: MessageFns<EvaluateInt64FeatureFlagResponse> = {
  encode(message: EvaluateInt64FeatureFlagResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.value !== 0) {
      writer.uint32(16).int64(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EvaluateInt64FeatureFlagResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvaluateInt64FeatureFlagResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EvaluateInt64FeatureFlagResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: EvaluateInt64FeatureFlagResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EvaluateInt64FeatureFlagResponse>, I>>(
    base?: I,
  ): EvaluateInt64FeatureFlagResponse {
    return EvaluateInt64FeatureFlagResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EvaluateInt64FeatureFlagResponse>, I>>(
    object: I,
  ): EvaluateInt64FeatureFlagResponse {
    const message = createBaseEvaluateInt64FeatureFlagResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseEvaluateStringFeatureFlagRequest(): EvaluateStringFeatureFlagRequest {
  return { featureFlag: '' };
}

export const EvaluateStringFeatureFlagRequest: MessageFns<EvaluateStringFeatureFlagRequest> = {
  encode(message: EvaluateStringFeatureFlagRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.featureFlag !== '') {
      writer.uint32(10).string(message.featureFlag);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EvaluateStringFeatureFlagRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvaluateStringFeatureFlagRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.featureFlag = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EvaluateStringFeatureFlagRequest {
    return {
      featureFlag: isSet(object.featureFlag)
        ? globalThis.String(object.featureFlag)
        : isSet(object.feature_flag)
          ? globalThis.String(object.feature_flag)
          : '',
    };
  },

  toJSON(message: EvaluateStringFeatureFlagRequest): unknown {
    const obj: any = {};
    if (message.featureFlag !== '') {
      obj.featureFlag = message.featureFlag;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EvaluateStringFeatureFlagRequest>, I>>(
    base?: I,
  ): EvaluateStringFeatureFlagRequest {
    return EvaluateStringFeatureFlagRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EvaluateStringFeatureFlagRequest>, I>>(
    object: I,
  ): EvaluateStringFeatureFlagRequest {
    const message = createBaseEvaluateStringFeatureFlagRequest();
    message.featureFlag = object.featureFlag ?? '';
    return message;
  },
};

function createBaseEvaluateStringFeatureFlagResponse(): EvaluateStringFeatureFlagResponse {
  return { responseDetails: undefined, value: '' };
}

export const EvaluateStringFeatureFlagResponse: MessageFns<EvaluateStringFeatureFlagResponse> = {
  encode(message: EvaluateStringFeatureFlagResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.value !== '') {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EvaluateStringFeatureFlagResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvaluateStringFeatureFlagResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EvaluateStringFeatureFlagResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      value: isSet(object.value) ? globalThis.String(object.value) : '',
    };
  },

  toJSON(message: EvaluateStringFeatureFlagResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.value !== '') {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EvaluateStringFeatureFlagResponse>, I>>(
    base?: I,
  ): EvaluateStringFeatureFlagResponse {
    return EvaluateStringFeatureFlagResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EvaluateStringFeatureFlagResponse>, I>>(
    object: I,
  ): EvaluateStringFeatureFlagResponse {
    const message = createBaseEvaluateStringFeatureFlagResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.value = object.value ?? '';
    return message;
  },
};

function createBaseBeginPasskeyRegistrationRequest(): BeginPasskeyRegistrationRequest {
  return {};
}

export const BeginPasskeyRegistrationRequest: MessageFns<BeginPasskeyRegistrationRequest> = {
  encode(_: BeginPasskeyRegistrationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BeginPasskeyRegistrationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBeginPasskeyRegistrationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): BeginPasskeyRegistrationRequest {
    return {};
  },

  toJSON(_: BeginPasskeyRegistrationRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<BeginPasskeyRegistrationRequest>, I>>(base?: I): BeginPasskeyRegistrationRequest {
    return BeginPasskeyRegistrationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BeginPasskeyRegistrationRequest>, I>>(_: I): BeginPasskeyRegistrationRequest {
    const message = createBaseBeginPasskeyRegistrationRequest();
    return message;
  },
};

function createBaseBeginPasskeyRegistrationResponse(): BeginPasskeyRegistrationResponse {
  return { responseDetails: undefined, publicKeyCredentialCreationOptions: new Uint8Array(0), challenge: '' };
}

export const BeginPasskeyRegistrationResponse: MessageFns<BeginPasskeyRegistrationResponse> = {
  encode(message: BeginPasskeyRegistrationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.publicKeyCredentialCreationOptions.length !== 0) {
      writer.uint32(18).bytes(message.publicKeyCredentialCreationOptions);
    }
    if (message.challenge !== '') {
      writer.uint32(26).string(message.challenge);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BeginPasskeyRegistrationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBeginPasskeyRegistrationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.publicKeyCredentialCreationOptions = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.challenge = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BeginPasskeyRegistrationResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      publicKeyCredentialCreationOptions: isSet(object.publicKeyCredentialCreationOptions)
        ? bytesFromBase64(object.publicKeyCredentialCreationOptions)
        : isSet(object.public_key_credential_creation_options)
          ? bytesFromBase64(object.public_key_credential_creation_options)
          : new Uint8Array(0),
      challenge: isSet(object.challenge) ? globalThis.String(object.challenge) : '',
    };
  },

  toJSON(message: BeginPasskeyRegistrationResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.publicKeyCredentialCreationOptions.length !== 0) {
      obj.publicKeyCredentialCreationOptions = base64FromBytes(message.publicKeyCredentialCreationOptions);
    }
    if (message.challenge !== '') {
      obj.challenge = message.challenge;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BeginPasskeyRegistrationResponse>, I>>(
    base?: I,
  ): BeginPasskeyRegistrationResponse {
    return BeginPasskeyRegistrationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BeginPasskeyRegistrationResponse>, I>>(
    object: I,
  ): BeginPasskeyRegistrationResponse {
    const message = createBaseBeginPasskeyRegistrationResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.publicKeyCredentialCreationOptions = object.publicKeyCredentialCreationOptions ?? new Uint8Array(0);
    message.challenge = object.challenge ?? '';
    return message;
  },
};

function createBaseFinishPasskeyRegistrationRequest(): FinishPasskeyRegistrationRequest {
  return { attestationResponse: new Uint8Array(0), challenge: '' };
}

export const FinishPasskeyRegistrationRequest: MessageFns<FinishPasskeyRegistrationRequest> = {
  encode(message: FinishPasskeyRegistrationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.attestationResponse.length !== 0) {
      writer.uint32(10).bytes(message.attestationResponse);
    }
    if (message.challenge !== '') {
      writer.uint32(18).string(message.challenge);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FinishPasskeyRegistrationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFinishPasskeyRegistrationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.attestationResponse = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.challenge = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FinishPasskeyRegistrationRequest {
    return {
      attestationResponse: isSet(object.attestationResponse)
        ? bytesFromBase64(object.attestationResponse)
        : isSet(object.attestation_response)
          ? bytesFromBase64(object.attestation_response)
          : new Uint8Array(0),
      challenge: isSet(object.challenge) ? globalThis.String(object.challenge) : '',
    };
  },

  toJSON(message: FinishPasskeyRegistrationRequest): unknown {
    const obj: any = {};
    if (message.attestationResponse.length !== 0) {
      obj.attestationResponse = base64FromBytes(message.attestationResponse);
    }
    if (message.challenge !== '') {
      obj.challenge = message.challenge;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FinishPasskeyRegistrationRequest>, I>>(
    base?: I,
  ): FinishPasskeyRegistrationRequest {
    return FinishPasskeyRegistrationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FinishPasskeyRegistrationRequest>, I>>(
    object: I,
  ): FinishPasskeyRegistrationRequest {
    const message = createBaseFinishPasskeyRegistrationRequest();
    message.attestationResponse = object.attestationResponse ?? new Uint8Array(0);
    message.challenge = object.challenge ?? '';
    return message;
  },
};

function createBaseFinishPasskeyRegistrationResponse(): FinishPasskeyRegistrationResponse {
  return { responseDetails: undefined };
}

export const FinishPasskeyRegistrationResponse: MessageFns<FinishPasskeyRegistrationResponse> = {
  encode(message: FinishPasskeyRegistrationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FinishPasskeyRegistrationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFinishPasskeyRegistrationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FinishPasskeyRegistrationResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
    };
  },

  toJSON(message: FinishPasskeyRegistrationResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FinishPasskeyRegistrationResponse>, I>>(
    base?: I,
  ): FinishPasskeyRegistrationResponse {
    return FinishPasskeyRegistrationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FinishPasskeyRegistrationResponse>, I>>(
    object: I,
  ): FinishPasskeyRegistrationResponse {
    const message = createBaseFinishPasskeyRegistrationResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    return message;
  },
};

function createBaseBeginPasskeyAuthenticationRequest(): BeginPasskeyAuthenticationRequest {
  return { username: '' };
}

export const BeginPasskeyAuthenticationRequest: MessageFns<BeginPasskeyAuthenticationRequest> = {
  encode(message: BeginPasskeyAuthenticationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== '') {
      writer.uint32(10).string(message.username);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BeginPasskeyAuthenticationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBeginPasskeyAuthenticationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.username = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BeginPasskeyAuthenticationRequest {
    return { username: isSet(object.username) ? globalThis.String(object.username) : '' };
  },

  toJSON(message: BeginPasskeyAuthenticationRequest): unknown {
    const obj: any = {};
    if (message.username !== '') {
      obj.username = message.username;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BeginPasskeyAuthenticationRequest>, I>>(
    base?: I,
  ): BeginPasskeyAuthenticationRequest {
    return BeginPasskeyAuthenticationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BeginPasskeyAuthenticationRequest>, I>>(
    object: I,
  ): BeginPasskeyAuthenticationRequest {
    const message = createBaseBeginPasskeyAuthenticationRequest();
    message.username = object.username ?? '';
    return message;
  },
};

function createBaseBeginPasskeyAuthenticationResponse(): BeginPasskeyAuthenticationResponse {
  return { responseDetails: undefined, publicKeyCredentialRequestOptions: new Uint8Array(0), challenge: '' };
}

export const BeginPasskeyAuthenticationResponse: MessageFns<BeginPasskeyAuthenticationResponse> = {
  encode(message: BeginPasskeyAuthenticationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.publicKeyCredentialRequestOptions.length !== 0) {
      writer.uint32(18).bytes(message.publicKeyCredentialRequestOptions);
    }
    if (message.challenge !== '') {
      writer.uint32(26).string(message.challenge);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BeginPasskeyAuthenticationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBeginPasskeyAuthenticationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.publicKeyCredentialRequestOptions = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.challenge = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BeginPasskeyAuthenticationResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      publicKeyCredentialRequestOptions: isSet(object.publicKeyCredentialRequestOptions)
        ? bytesFromBase64(object.publicKeyCredentialRequestOptions)
        : isSet(object.public_key_credential_request_options)
          ? bytesFromBase64(object.public_key_credential_request_options)
          : new Uint8Array(0),
      challenge: isSet(object.challenge) ? globalThis.String(object.challenge) : '',
    };
  },

  toJSON(message: BeginPasskeyAuthenticationResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.publicKeyCredentialRequestOptions.length !== 0) {
      obj.publicKeyCredentialRequestOptions = base64FromBytes(message.publicKeyCredentialRequestOptions);
    }
    if (message.challenge !== '') {
      obj.challenge = message.challenge;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BeginPasskeyAuthenticationResponse>, I>>(
    base?: I,
  ): BeginPasskeyAuthenticationResponse {
    return BeginPasskeyAuthenticationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BeginPasskeyAuthenticationResponse>, I>>(
    object: I,
  ): BeginPasskeyAuthenticationResponse {
    const message = createBaseBeginPasskeyAuthenticationResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.publicKeyCredentialRequestOptions = object.publicKeyCredentialRequestOptions ?? new Uint8Array(0);
    message.challenge = object.challenge ?? '';
    return message;
  },
};

function createBaseFinishPasskeyAuthenticationRequest(): FinishPasskeyAuthenticationRequest {
  return { assertionResponse: new Uint8Array(0), challenge: '', username: '' };
}

export const FinishPasskeyAuthenticationRequest: MessageFns<FinishPasskeyAuthenticationRequest> = {
  encode(message: FinishPasskeyAuthenticationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.assertionResponse.length !== 0) {
      writer.uint32(10).bytes(message.assertionResponse);
    }
    if (message.challenge !== '') {
      writer.uint32(18).string(message.challenge);
    }
    if (message.username !== '') {
      writer.uint32(26).string(message.username);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FinishPasskeyAuthenticationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFinishPasskeyAuthenticationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.assertionResponse = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.challenge = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.username = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FinishPasskeyAuthenticationRequest {
    return {
      assertionResponse: isSet(object.assertionResponse)
        ? bytesFromBase64(object.assertionResponse)
        : isSet(object.assertion_response)
          ? bytesFromBase64(object.assertion_response)
          : new Uint8Array(0),
      challenge: isSet(object.challenge) ? globalThis.String(object.challenge) : '',
      username: isSet(object.username) ? globalThis.String(object.username) : '',
    };
  },

  toJSON(message: FinishPasskeyAuthenticationRequest): unknown {
    const obj: any = {};
    if (message.assertionResponse.length !== 0) {
      obj.assertionResponse = base64FromBytes(message.assertionResponse);
    }
    if (message.challenge !== '') {
      obj.challenge = message.challenge;
    }
    if (message.username !== '') {
      obj.username = message.username;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FinishPasskeyAuthenticationRequest>, I>>(
    base?: I,
  ): FinishPasskeyAuthenticationRequest {
    return FinishPasskeyAuthenticationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FinishPasskeyAuthenticationRequest>, I>>(
    object: I,
  ): FinishPasskeyAuthenticationRequest {
    const message = createBaseFinishPasskeyAuthenticationRequest();
    message.assertionResponse = object.assertionResponse ?? new Uint8Array(0);
    message.challenge = object.challenge ?? '';
    message.username = object.username ?? '';
    return message;
  },
};

function createBaseGetSelfRequest(): GetSelfRequest {
  return {};
}

export const GetSelfRequest: MessageFns<GetSelfRequest> = {
  encode(_: GetSelfRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSelfRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSelfRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetSelfRequest {
    return {};
  },

  toJSON(_: GetSelfRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSelfRequest>, I>>(base?: I): GetSelfRequest {
    return GetSelfRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSelfRequest>, I>>(_: I): GetSelfRequest {
    const message = createBaseGetSelfRequest();
    return message;
  },
};

function createBaseGetSelfResponse(): GetSelfResponse {
  return { responseDetails: undefined, result: undefined };
}

export const GetSelfResponse: MessageFns<GetSelfResponse> = {
  encode(message: GetSelfResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.result !== undefined) {
      User.encode(message.result, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSelfResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSelfResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.result = User.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSelfResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      result: isSet(object.result) ? User.fromJSON(object.result) : undefined,
    };
  },

  toJSON(message: GetSelfResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.result !== undefined) {
      obj.result = User.toJSON(message.result);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSelfResponse>, I>>(base?: I): GetSelfResponse {
    return GetSelfResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSelfResponse>, I>>(object: I): GetSelfResponse {
    const message = createBaseGetSelfResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.result =
      object.result !== undefined && object.result !== null ? User.fromPartial(object.result) : undefined;
    return message;
  },
};

function createBaseListPasskeysRequest(): ListPasskeysRequest {
  return {};
}

export const ListPasskeysRequest: MessageFns<ListPasskeysRequest> = {
  encode(_: ListPasskeysRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPasskeysRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPasskeysRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListPasskeysRequest {
    return {};
  },

  toJSON(_: ListPasskeysRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListPasskeysRequest>, I>>(base?: I): ListPasskeysRequest {
    return ListPasskeysRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListPasskeysRequest>, I>>(_: I): ListPasskeysRequest {
    const message = createBaseListPasskeysRequest();
    return message;
  },
};

function createBaseListPasskeysResponse(): ListPasskeysResponse {
  return { responseDetails: undefined, results: [] };
}

export const ListPasskeysResponse: MessageFns<ListPasskeysResponse> = {
  encode(message: ListPasskeysResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    for (const v of message.results) {
      PasskeyCredential.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPasskeysResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPasskeysResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.results.push(PasskeyCredential.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPasskeysResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      results: globalThis.Array.isArray(object?.results)
        ? object.results.map((e: any) => PasskeyCredential.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListPasskeysResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.results?.length) {
      obj.results = message.results.map((e) => PasskeyCredential.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListPasskeysResponse>, I>>(base?: I): ListPasskeysResponse {
    return ListPasskeysResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListPasskeysResponse>, I>>(object: I): ListPasskeysResponse {
    const message = createBaseListPasskeysResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.results = object.results?.map((e) => PasskeyCredential.fromPartial(e)) || [];
    return message;
  },
};

function createBasePasskeyCredential(): PasskeyCredential {
  return { id: '', friendlyName: '', createdAt: undefined, lastUsedAt: undefined };
}

export const PasskeyCredential: MessageFns<PasskeyCredential> = {
  encode(message: PasskeyCredential, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.friendlyName !== '') {
      writer.uint32(18).string(message.friendlyName);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(26).fork()).join();
    }
    if (message.lastUsedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUsedAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PasskeyCredential {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePasskeyCredential();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.friendlyName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.lastUsedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PasskeyCredential {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      friendlyName: isSet(object.friendlyName)
        ? globalThis.String(object.friendlyName)
        : isSet(object.friendly_name)
          ? globalThis.String(object.friendly_name)
          : '',
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      lastUsedAt: isSet(object.lastUsedAt)
        ? fromJsonTimestamp(object.lastUsedAt)
        : isSet(object.last_used_at)
          ? fromJsonTimestamp(object.last_used_at)
          : undefined,
    };
  },

  toJSON(message: PasskeyCredential): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.friendlyName !== '') {
      obj.friendlyName = message.friendlyName;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.lastUsedAt !== undefined) {
      obj.lastUsedAt = message.lastUsedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PasskeyCredential>, I>>(base?: I): PasskeyCredential {
    return PasskeyCredential.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PasskeyCredential>, I>>(object: I): PasskeyCredential {
    const message = createBasePasskeyCredential();
    message.id = object.id ?? '';
    message.friendlyName = object.friendlyName ?? '';
    message.createdAt = object.createdAt ?? undefined;
    message.lastUsedAt = object.lastUsedAt ?? undefined;
    return message;
  },
};

function createBaseArchivePasskeyRequest(): ArchivePasskeyRequest {
  return { credentialId: '' };
}

export const ArchivePasskeyRequest: MessageFns<ArchivePasskeyRequest> = {
  encode(message: ArchivePasskeyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.credentialId !== '') {
      writer.uint32(10).string(message.credentialId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchivePasskeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchivePasskeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.credentialId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArchivePasskeyRequest {
    return {
      credentialId: isSet(object.credentialId)
        ? globalThis.String(object.credentialId)
        : isSet(object.credential_id)
          ? globalThis.String(object.credential_id)
          : '',
    };
  },

  toJSON(message: ArchivePasskeyRequest): unknown {
    const obj: any = {};
    if (message.credentialId !== '') {
      obj.credentialId = message.credentialId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArchivePasskeyRequest>, I>>(base?: I): ArchivePasskeyRequest {
    return ArchivePasskeyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArchivePasskeyRequest>, I>>(object: I): ArchivePasskeyRequest {
    const message = createBaseArchivePasskeyRequest();
    message.credentialId = object.credentialId ?? '';
    return message;
  },
};

function createBaseArchivePasskeyResponse(): ArchivePasskeyResponse {
  return { responseDetails: undefined };
}

export const ArchivePasskeyResponse: MessageFns<ArchivePasskeyResponse> = {
  encode(message: ArchivePasskeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchivePasskeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchivePasskeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArchivePasskeyResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
    };
  },

  toJSON(message: ArchivePasskeyResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArchivePasskeyResponse>, I>>(base?: I): ArchivePasskeyResponse {
    return ArchivePasskeyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArchivePasskeyResponse>, I>>(object: I): ArchivePasskeyResponse {
    const message = createBaseArchivePasskeyResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    return message;
  },
};

function createBaseListActiveSessionsRequest(): ListActiveSessionsRequest {
  return { filter: undefined };
}

export const ListActiveSessionsRequest: MessageFns<ListActiveSessionsRequest> = {
  encode(message: ListActiveSessionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filter !== undefined) {
      QueryFilter.encode(message.filter, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListActiveSessionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListActiveSessionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filter = QueryFilter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListActiveSessionsRequest {
    return { filter: isSet(object.filter) ? QueryFilter.fromJSON(object.filter) : undefined };
  },

  toJSON(message: ListActiveSessionsRequest): unknown {
    const obj: any = {};
    if (message.filter !== undefined) {
      obj.filter = QueryFilter.toJSON(message.filter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListActiveSessionsRequest>, I>>(base?: I): ListActiveSessionsRequest {
    return ListActiveSessionsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListActiveSessionsRequest>, I>>(object: I): ListActiveSessionsRequest {
    const message = createBaseListActiveSessionsRequest();
    message.filter =
      object.filter !== undefined && object.filter !== null ? QueryFilter.fromPartial(object.filter) : undefined;
    return message;
  },
};

function createBaseListActiveSessionsResponse(): ListActiveSessionsResponse {
  return { responseDetails: undefined, pagination: undefined, sessions: [] };
}

export const ListActiveSessionsResponse: MessageFns<ListActiveSessionsResponse> = {
  encode(message: ListActiveSessionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      Pagination.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    for (const v of message.sessions) {
      UserSession.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListActiveSessionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListActiveSessionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = Pagination.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sessions.push(UserSession.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListActiveSessionsResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      pagination: isSet(object.pagination) ? Pagination.fromJSON(object.pagination) : undefined,
      sessions: globalThis.Array.isArray(object?.sessions)
        ? object.sessions.map((e: any) => UserSession.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListActiveSessionsResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.pagination !== undefined) {
      obj.pagination = Pagination.toJSON(message.pagination);
    }
    if (message.sessions?.length) {
      obj.sessions = message.sessions.map((e) => UserSession.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListActiveSessionsResponse>, I>>(base?: I): ListActiveSessionsResponse {
    return ListActiveSessionsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListActiveSessionsResponse>, I>>(object: I): ListActiveSessionsResponse {
    const message = createBaseListActiveSessionsResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? Pagination.fromPartial(object.pagination)
        : undefined;
    message.sessions = object.sessions?.map((e) => UserSession.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRevokeSessionRequest(): RevokeSessionRequest {
  return { sessionId: '' };
}

export const RevokeSessionRequest: MessageFns<RevokeSessionRequest> = {
  encode(message: RevokeSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== '') {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevokeSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevokeSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RevokeSessionRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
          ? globalThis.String(object.session_id)
          : '',
    };
  },

  toJSON(message: RevokeSessionRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== '') {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RevokeSessionRequest>, I>>(base?: I): RevokeSessionRequest {
    return RevokeSessionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RevokeSessionRequest>, I>>(object: I): RevokeSessionRequest {
    const message = createBaseRevokeSessionRequest();
    message.sessionId = object.sessionId ?? '';
    return message;
  },
};

function createBaseRevokeSessionResponse(): RevokeSessionResponse {
  return { responseDetails: undefined };
}

export const RevokeSessionResponse: MessageFns<RevokeSessionResponse> = {
  encode(message: RevokeSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevokeSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevokeSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RevokeSessionResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
    };
  },

  toJSON(message: RevokeSessionResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RevokeSessionResponse>, I>>(base?: I): RevokeSessionResponse {
    return RevokeSessionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RevokeSessionResponse>, I>>(object: I): RevokeSessionResponse {
    const message = createBaseRevokeSessionResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    return message;
  },
};

function createBaseRevokeAllOtherSessionsRequest(): RevokeAllOtherSessionsRequest {
  return {};
}

export const RevokeAllOtherSessionsRequest: MessageFns<RevokeAllOtherSessionsRequest> = {
  encode(_: RevokeAllOtherSessionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevokeAllOtherSessionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevokeAllOtherSessionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RevokeAllOtherSessionsRequest {
    return {};
  },

  toJSON(_: RevokeAllOtherSessionsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<RevokeAllOtherSessionsRequest>, I>>(base?: I): RevokeAllOtherSessionsRequest {
    return RevokeAllOtherSessionsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RevokeAllOtherSessionsRequest>, I>>(_: I): RevokeAllOtherSessionsRequest {
    const message = createBaseRevokeAllOtherSessionsRequest();
    return message;
  },
};

function createBaseRevokeAllOtherSessionsResponse(): RevokeAllOtherSessionsResponse {
  return { responseDetails: undefined };
}

export const RevokeAllOtherSessionsResponse: MessageFns<RevokeAllOtherSessionsResponse> = {
  encode(message: RevokeAllOtherSessionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevokeAllOtherSessionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevokeAllOtherSessionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RevokeAllOtherSessionsResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
    };
  },

  toJSON(message: RevokeAllOtherSessionsResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RevokeAllOtherSessionsResponse>, I>>(base?: I): RevokeAllOtherSessionsResponse {
    return RevokeAllOtherSessionsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RevokeAllOtherSessionsResponse>, I>>(
    object: I,
  ): RevokeAllOtherSessionsResponse {
    const message = createBaseRevokeAllOtherSessionsResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    return message;
  },
};

function createBaseRevokeCurrentSessionRequest(): RevokeCurrentSessionRequest {
  return {};
}

export const RevokeCurrentSessionRequest: MessageFns<RevokeCurrentSessionRequest> = {
  encode(_: RevokeCurrentSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevokeCurrentSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevokeCurrentSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RevokeCurrentSessionRequest {
    return {};
  },

  toJSON(_: RevokeCurrentSessionRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<RevokeCurrentSessionRequest>, I>>(base?: I): RevokeCurrentSessionRequest {
    return RevokeCurrentSessionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RevokeCurrentSessionRequest>, I>>(_: I): RevokeCurrentSessionRequest {
    const message = createBaseRevokeCurrentSessionRequest();
    return message;
  },
};

function createBaseRevokeCurrentSessionResponse(): RevokeCurrentSessionResponse {
  return { responseDetails: undefined };
}

export const RevokeCurrentSessionResponse: MessageFns<RevokeCurrentSessionResponse> = {
  encode(message: RevokeCurrentSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevokeCurrentSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevokeCurrentSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RevokeCurrentSessionResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
    };
  },

  toJSON(message: RevokeCurrentSessionResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RevokeCurrentSessionResponse>, I>>(base?: I): RevokeCurrentSessionResponse {
    return RevokeCurrentSessionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RevokeCurrentSessionResponse>, I>>(object: I): RevokeCurrentSessionResponse {
    const message = createBaseRevokeCurrentSessionResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    return message;
  },
};

function createBaseAdminListSessionsForUserRequest(): AdminListSessionsForUserRequest {
  return { userId: '', filter: undefined };
}

export const AdminListSessionsForUserRequest: MessageFns<AdminListSessionsForUserRequest> = {
  encode(message: AdminListSessionsForUserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== '') {
      writer.uint32(10).string(message.userId);
    }
    if (message.filter !== undefined) {
      QueryFilter.encode(message.filter, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AdminListSessionsForUserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAdminListSessionsForUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filter = QueryFilter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AdminListSessionsForUserRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
      filter: isSet(object.filter) ? QueryFilter.fromJSON(object.filter) : undefined,
    };
  },

  toJSON(message: AdminListSessionsForUserRequest): unknown {
    const obj: any = {};
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    if (message.filter !== undefined) {
      obj.filter = QueryFilter.toJSON(message.filter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AdminListSessionsForUserRequest>, I>>(base?: I): AdminListSessionsForUserRequest {
    return AdminListSessionsForUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AdminListSessionsForUserRequest>, I>>(
    object: I,
  ): AdminListSessionsForUserRequest {
    const message = createBaseAdminListSessionsForUserRequest();
    message.userId = object.userId ?? '';
    message.filter =
      object.filter !== undefined && object.filter !== null ? QueryFilter.fromPartial(object.filter) : undefined;
    return message;
  },
};

function createBaseAdminRevokeUserSessionRequest(): AdminRevokeUserSessionRequest {
  return { userId: '', sessionId: '' };
}

export const AdminRevokeUserSessionRequest: MessageFns<AdminRevokeUserSessionRequest> = {
  encode(message: AdminRevokeUserSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== '') {
      writer.uint32(10).string(message.userId);
    }
    if (message.sessionId !== '') {
      writer.uint32(18).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AdminRevokeUserSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAdminRevokeUserSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AdminRevokeUserSessionRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
          ? globalThis.String(object.session_id)
          : '',
    };
  },

  toJSON(message: AdminRevokeUserSessionRequest): unknown {
    const obj: any = {};
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    if (message.sessionId !== '') {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AdminRevokeUserSessionRequest>, I>>(base?: I): AdminRevokeUserSessionRequest {
    return AdminRevokeUserSessionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AdminRevokeUserSessionRequest>, I>>(
    object: I,
  ): AdminRevokeUserSessionRequest {
    const message = createBaseAdminRevokeUserSessionRequest();
    message.userId = object.userId ?? '';
    message.sessionId = object.sessionId ?? '';
    return message;
  },
};

function createBaseAdminRevokeAllUserSessionsRequest(): AdminRevokeAllUserSessionsRequest {
  return { userId: '' };
}

export const AdminRevokeAllUserSessionsRequest: MessageFns<AdminRevokeAllUserSessionsRequest> = {
  encode(message: AdminRevokeAllUserSessionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== '') {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AdminRevokeAllUserSessionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAdminRevokeAllUserSessionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AdminRevokeAllUserSessionsRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
    };
  },

  toJSON(message: AdminRevokeAllUserSessionsRequest): unknown {
    const obj: any = {};
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AdminRevokeAllUserSessionsRequest>, I>>(
    base?: I,
  ): AdminRevokeAllUserSessionsRequest {
    return AdminRevokeAllUserSessionsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AdminRevokeAllUserSessionsRequest>, I>>(
    object: I,
  ): AdminRevokeAllUserSessionsRequest {
    const message = createBaseAdminRevokeAllUserSessionsRequest();
    message.userId = object.userId ?? '';
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, 'base64'));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString('base64');
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(''));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === 'string') {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error('Value is larger than Number.MAX_SAFE_INTEGER');
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error('Value is smaller than Number.MIN_SAFE_INTEGER');
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === 'object' && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
