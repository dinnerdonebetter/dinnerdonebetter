// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.1
// source: analytics/analytics_service_types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { ResponseDetails } from '../common';

export const protobufPackage = 'analytics';

export interface TrackEventRequest {
  source: string;
  event: string;
  properties: { [key: string]: string };
}

export interface TrackEventRequest_PropertiesEntry {
  key: string;
  value: string;
}

export interface TrackEventResponse {
  responseDetails: ResponseDetails | undefined;
}

export interface TrackAnonymousEventRequest {
  source: string;
  event: string;
  anonymousId: string;
  properties: { [key: string]: string };
}

export interface TrackAnonymousEventRequest_PropertiesEntry {
  key: string;
  value: string;
}

export interface TrackAnonymousEventResponse {
  responseDetails: ResponseDetails | undefined;
}

function createBaseTrackEventRequest(): TrackEventRequest {
  return { source: '', event: '', properties: {} };
}

export const TrackEventRequest: MessageFns<TrackEventRequest> = {
  encode(message: TrackEventRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== '') {
      writer.uint32(10).string(message.source);
    }
    if (message.event !== '') {
      writer.uint32(18).string(message.event);
    }
    globalThis.Object.entries(message.properties).forEach(([key, value]: [string, string]) => {
      TrackEventRequest_PropertiesEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrackEventRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrackEventRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.event = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = TrackEventRequest_PropertiesEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.properties[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrackEventRequest {
    return {
      source: isSet(object.source) ? globalThis.String(object.source) : '',
      event: isSet(object.event) ? globalThis.String(object.event) : '',
      properties: isObject(object.properties)
        ? (globalThis.Object.entries(object.properties) as [string, any][]).reduce(
            (acc: { [key: string]: string }, [key, value]: [string, any]) => {
              acc[key] = globalThis.String(value);
              return acc;
            },
            {},
          )
        : {},
    };
  },

  toJSON(message: TrackEventRequest): unknown {
    const obj: any = {};
    if (message.source !== '') {
      obj.source = message.source;
    }
    if (message.event !== '') {
      obj.event = message.event;
    }
    if (message.properties) {
      const entries = globalThis.Object.entries(message.properties) as [string, string][];
      if (entries.length > 0) {
        obj.properties = {};
        entries.forEach(([k, v]) => {
          obj.properties[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrackEventRequest>, I>>(base?: I): TrackEventRequest {
    return TrackEventRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrackEventRequest>, I>>(object: I): TrackEventRequest {
    const message = createBaseTrackEventRequest();
    message.source = object.source ?? '';
    message.event = object.event ?? '';
    message.properties = (globalThis.Object.entries(object.properties ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseTrackEventRequest_PropertiesEntry(): TrackEventRequest_PropertiesEntry {
  return { key: '', value: '' };
}

export const TrackEventRequest_PropertiesEntry: MessageFns<TrackEventRequest_PropertiesEntry> = {
  encode(message: TrackEventRequest_PropertiesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== '') {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== '') {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrackEventRequest_PropertiesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrackEventRequest_PropertiesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrackEventRequest_PropertiesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : '',
      value: isSet(object.value) ? globalThis.String(object.value) : '',
    };
  },

  toJSON(message: TrackEventRequest_PropertiesEntry): unknown {
    const obj: any = {};
    if (message.key !== '') {
      obj.key = message.key;
    }
    if (message.value !== '') {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrackEventRequest_PropertiesEntry>, I>>(
    base?: I,
  ): TrackEventRequest_PropertiesEntry {
    return TrackEventRequest_PropertiesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrackEventRequest_PropertiesEntry>, I>>(
    object: I,
  ): TrackEventRequest_PropertiesEntry {
    const message = createBaseTrackEventRequest_PropertiesEntry();
    message.key = object.key ?? '';
    message.value = object.value ?? '';
    return message;
  },
};

function createBaseTrackEventResponse(): TrackEventResponse {
  return { responseDetails: undefined };
}

export const TrackEventResponse: MessageFns<TrackEventResponse> = {
  encode(message: TrackEventResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrackEventResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrackEventResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrackEventResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
    };
  },

  toJSON(message: TrackEventResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrackEventResponse>, I>>(base?: I): TrackEventResponse {
    return TrackEventResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrackEventResponse>, I>>(object: I): TrackEventResponse {
    const message = createBaseTrackEventResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    return message;
  },
};

function createBaseTrackAnonymousEventRequest(): TrackAnonymousEventRequest {
  return { source: '', event: '', anonymousId: '', properties: {} };
}

export const TrackAnonymousEventRequest: MessageFns<TrackAnonymousEventRequest> = {
  encode(message: TrackAnonymousEventRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== '') {
      writer.uint32(10).string(message.source);
    }
    if (message.event !== '') {
      writer.uint32(18).string(message.event);
    }
    if (message.anonymousId !== '') {
      writer.uint32(26).string(message.anonymousId);
    }
    globalThis.Object.entries(message.properties).forEach(([key, value]: [string, string]) => {
      TrackAnonymousEventRequest_PropertiesEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrackAnonymousEventRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrackAnonymousEventRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.event = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.anonymousId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = TrackAnonymousEventRequest_PropertiesEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.properties[entry4.key] = entry4.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrackAnonymousEventRequest {
    return {
      source: isSet(object.source) ? globalThis.String(object.source) : '',
      event: isSet(object.event) ? globalThis.String(object.event) : '',
      anonymousId: isSet(object.anonymousId)
        ? globalThis.String(object.anonymousId)
        : isSet(object.anonymous_id)
          ? globalThis.String(object.anonymous_id)
          : '',
      properties: isObject(object.properties)
        ? (globalThis.Object.entries(object.properties) as [string, any][]).reduce(
            (acc: { [key: string]: string }, [key, value]: [string, any]) => {
              acc[key] = globalThis.String(value);
              return acc;
            },
            {},
          )
        : {},
    };
  },

  toJSON(message: TrackAnonymousEventRequest): unknown {
    const obj: any = {};
    if (message.source !== '') {
      obj.source = message.source;
    }
    if (message.event !== '') {
      obj.event = message.event;
    }
    if (message.anonymousId !== '') {
      obj.anonymousId = message.anonymousId;
    }
    if (message.properties) {
      const entries = globalThis.Object.entries(message.properties) as [string, string][];
      if (entries.length > 0) {
        obj.properties = {};
        entries.forEach(([k, v]) => {
          obj.properties[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrackAnonymousEventRequest>, I>>(base?: I): TrackAnonymousEventRequest {
    return TrackAnonymousEventRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrackAnonymousEventRequest>, I>>(object: I): TrackAnonymousEventRequest {
    const message = createBaseTrackAnonymousEventRequest();
    message.source = object.source ?? '';
    message.event = object.event ?? '';
    message.anonymousId = object.anonymousId ?? '';
    message.properties = (globalThis.Object.entries(object.properties ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseTrackAnonymousEventRequest_PropertiesEntry(): TrackAnonymousEventRequest_PropertiesEntry {
  return { key: '', value: '' };
}

export const TrackAnonymousEventRequest_PropertiesEntry: MessageFns<TrackAnonymousEventRequest_PropertiesEntry> = {
  encode(message: TrackAnonymousEventRequest_PropertiesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== '') {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== '') {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrackAnonymousEventRequest_PropertiesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrackAnonymousEventRequest_PropertiesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrackAnonymousEventRequest_PropertiesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : '',
      value: isSet(object.value) ? globalThis.String(object.value) : '',
    };
  },

  toJSON(message: TrackAnonymousEventRequest_PropertiesEntry): unknown {
    const obj: any = {};
    if (message.key !== '') {
      obj.key = message.key;
    }
    if (message.value !== '') {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrackAnonymousEventRequest_PropertiesEntry>, I>>(
    base?: I,
  ): TrackAnonymousEventRequest_PropertiesEntry {
    return TrackAnonymousEventRequest_PropertiesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrackAnonymousEventRequest_PropertiesEntry>, I>>(
    object: I,
  ): TrackAnonymousEventRequest_PropertiesEntry {
    const message = createBaseTrackAnonymousEventRequest_PropertiesEntry();
    message.key = object.key ?? '';
    message.value = object.value ?? '';
    return message;
  },
};

function createBaseTrackAnonymousEventResponse(): TrackAnonymousEventResponse {
  return { responseDetails: undefined };
}

export const TrackAnonymousEventResponse: MessageFns<TrackAnonymousEventResponse> = {
  encode(message: TrackAnonymousEventResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrackAnonymousEventResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrackAnonymousEventResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrackAnonymousEventResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
    };
  },

  toJSON(message: TrackAnonymousEventResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrackAnonymousEventResponse>, I>>(base?: I): TrackAnonymousEventResponse {
    return TrackAnonymousEventResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrackAnonymousEventResponse>, I>>(object: I): TrackAnonymousEventResponse {
    const message = createBaseTrackAnonymousEventResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === 'object' && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
