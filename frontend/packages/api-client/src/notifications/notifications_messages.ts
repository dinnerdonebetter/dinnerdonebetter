// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.1
// source: notifications/notifications_messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { Timestamp } from '../google/protobuf/timestamp';

export const protobufPackage = 'notifications';

export enum UserNotificationStatus {
  USER_NOTIFICATION_STATUS_UNREAD = 0,
  USER_NOTIFICATION_STATUS_READ = 1,
  USER_NOTIFICATION_STATUS_DISMISSED = 2,
  UNRECOGNIZED = -1,
}

export function userNotificationStatusFromJSON(object: any): UserNotificationStatus {
  switch (object) {
    case 0:
    case 'USER_NOTIFICATION_STATUS_UNREAD':
      return UserNotificationStatus.USER_NOTIFICATION_STATUS_UNREAD;
    case 1:
    case 'USER_NOTIFICATION_STATUS_READ':
      return UserNotificationStatus.USER_NOTIFICATION_STATUS_READ;
    case 2:
    case 'USER_NOTIFICATION_STATUS_DISMISSED':
      return UserNotificationStatus.USER_NOTIFICATION_STATUS_DISMISSED;
    case -1:
    case 'UNRECOGNIZED':
    default:
      return UserNotificationStatus.UNRECOGNIZED;
  }
}

export function userNotificationStatusToJSON(object: UserNotificationStatus): string {
  switch (object) {
    case UserNotificationStatus.USER_NOTIFICATION_STATUS_UNREAD:
      return 'USER_NOTIFICATION_STATUS_UNREAD';
    case UserNotificationStatus.USER_NOTIFICATION_STATUS_READ:
      return 'USER_NOTIFICATION_STATUS_READ';
    case UserNotificationStatus.USER_NOTIFICATION_STATUS_DISMISSED:
      return 'USER_NOTIFICATION_STATUS_DISMISSED';
    case UserNotificationStatus.UNRECOGNIZED:
    default:
      return 'UNRECOGNIZED';
  }
}

export interface DataCollection {
  notifications: UserNotification[];
}

export interface UserNotification {
  createdAt: Date | undefined;
  lastUpdatedAt: Date | undefined;
  id: string;
  content: string;
  status: UserNotificationStatus;
  belongsToUser: string;
}

export interface UserDeviceToken {
  createdAt: Date | undefined;
  lastUpdatedAt: Date | undefined;
  id: string;
  deviceToken: string;
  platform: string;
  belongsToUser: string;
}

function createBaseDataCollection(): DataCollection {
  return { notifications: [] };
}

export const DataCollection: MessageFns<DataCollection> = {
  encode(message: DataCollection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.notifications) {
      UserNotification.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataCollection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataCollection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.notifications.push(UserNotification.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataCollection {
    return {
      notifications: globalThis.Array.isArray(object?.notifications)
        ? object.notifications.map((e: any) => UserNotification.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DataCollection): unknown {
    const obj: any = {};
    if (message.notifications?.length) {
      obj.notifications = message.notifications.map((e) => UserNotification.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataCollection>, I>>(base?: I): DataCollection {
    return DataCollection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataCollection>, I>>(object: I): DataCollection {
    const message = createBaseDataCollection();
    message.notifications = object.notifications?.map((e) => UserNotification.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUserNotification(): UserNotification {
  return { createdAt: undefined, lastUpdatedAt: undefined, id: '', content: '', status: 0, belongsToUser: '' };
}

export const UserNotification: MessageFns<UserNotification> = {
  encode(message: UserNotification, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(10).fork()).join();
    }
    if (message.lastUpdatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUpdatedAt), writer.uint32(18).fork()).join();
    }
    if (message.id !== '') {
      writer.uint32(26).string(message.id);
    }
    if (message.content !== '') {
      writer.uint32(34).string(message.content);
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    if (message.belongsToUser !== '') {
      writer.uint32(50).string(message.belongsToUser);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserNotification {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserNotification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.lastUpdatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.belongsToUser = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserNotification {
    return {
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      lastUpdatedAt: isSet(object.lastUpdatedAt)
        ? fromJsonTimestamp(object.lastUpdatedAt)
        : isSet(object.last_updated_at)
          ? fromJsonTimestamp(object.last_updated_at)
          : undefined,
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      content: isSet(object.content) ? globalThis.String(object.content) : '',
      status: isSet(object.status) ? userNotificationStatusFromJSON(object.status) : 0,
      belongsToUser: isSet(object.belongsToUser)
        ? globalThis.String(object.belongsToUser)
        : isSet(object.belongs_to_user)
          ? globalThis.String(object.belongs_to_user)
          : '',
    };
  },

  toJSON(message: UserNotification): unknown {
    const obj: any = {};
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.lastUpdatedAt !== undefined) {
      obj.lastUpdatedAt = message.lastUpdatedAt.toISOString();
    }
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.content !== '') {
      obj.content = message.content;
    }
    if (message.status !== 0) {
      obj.status = userNotificationStatusToJSON(message.status);
    }
    if (message.belongsToUser !== '') {
      obj.belongsToUser = message.belongsToUser;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserNotification>, I>>(base?: I): UserNotification {
    return UserNotification.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserNotification>, I>>(object: I): UserNotification {
    const message = createBaseUserNotification();
    message.createdAt = object.createdAt ?? undefined;
    message.lastUpdatedAt = object.lastUpdatedAt ?? undefined;
    message.id = object.id ?? '';
    message.content = object.content ?? '';
    message.status = object.status ?? 0;
    message.belongsToUser = object.belongsToUser ?? '';
    return message;
  },
};

function createBaseUserDeviceToken(): UserDeviceToken {
  return { createdAt: undefined, lastUpdatedAt: undefined, id: '', deviceToken: '', platform: '', belongsToUser: '' };
}

export const UserDeviceToken: MessageFns<UserDeviceToken> = {
  encode(message: UserDeviceToken, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(10).fork()).join();
    }
    if (message.lastUpdatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUpdatedAt), writer.uint32(18).fork()).join();
    }
    if (message.id !== '') {
      writer.uint32(26).string(message.id);
    }
    if (message.deviceToken !== '') {
      writer.uint32(34).string(message.deviceToken);
    }
    if (message.platform !== '') {
      writer.uint32(42).string(message.platform);
    }
    if (message.belongsToUser !== '') {
      writer.uint32(50).string(message.belongsToUser);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserDeviceToken {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserDeviceToken();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.lastUpdatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.deviceToken = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.platform = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.belongsToUser = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserDeviceToken {
    return {
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      lastUpdatedAt: isSet(object.lastUpdatedAt)
        ? fromJsonTimestamp(object.lastUpdatedAt)
        : isSet(object.last_updated_at)
          ? fromJsonTimestamp(object.last_updated_at)
          : undefined,
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      deviceToken: isSet(object.deviceToken)
        ? globalThis.String(object.deviceToken)
        : isSet(object.device_token)
          ? globalThis.String(object.device_token)
          : '',
      platform: isSet(object.platform) ? globalThis.String(object.platform) : '',
      belongsToUser: isSet(object.belongsToUser)
        ? globalThis.String(object.belongsToUser)
        : isSet(object.belongs_to_user)
          ? globalThis.String(object.belongs_to_user)
          : '',
    };
  },

  toJSON(message: UserDeviceToken): unknown {
    const obj: any = {};
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.lastUpdatedAt !== undefined) {
      obj.lastUpdatedAt = message.lastUpdatedAt.toISOString();
    }
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.deviceToken !== '') {
      obj.deviceToken = message.deviceToken;
    }
    if (message.platform !== '') {
      obj.platform = message.platform;
    }
    if (message.belongsToUser !== '') {
      obj.belongsToUser = message.belongsToUser;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserDeviceToken>, I>>(base?: I): UserDeviceToken {
    return UserDeviceToken.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserDeviceToken>, I>>(object: I): UserDeviceToken {
    const message = createBaseUserDeviceToken();
    message.createdAt = object.createdAt ?? undefined;
    message.lastUpdatedAt = object.lastUpdatedAt ?? undefined;
    message.id = object.id ?? '';
    message.deviceToken = object.deviceToken ?? '';
    message.platform = object.platform ?? '';
    message.belongsToUser = object.belongsToUser ?? '';
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === 'string') {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
