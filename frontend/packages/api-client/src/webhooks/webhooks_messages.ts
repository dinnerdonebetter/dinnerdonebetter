// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.1
// source: webhooks/webhooks_messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { Timestamp } from '../google/protobuf/timestamp';

export const protobufPackage = 'webhooks';

export enum WebhookContentType {
  WEBHOOK_CONTENT_TYPE_JSON = 0,
  WEBHOOK_CONTENT_TYPE_XML = 1,
  UNRECOGNIZED = -1,
}

export function webhookContentTypeFromJSON(object: any): WebhookContentType {
  switch (object) {
    case 0:
    case 'WEBHOOK_CONTENT_TYPE_JSON':
      return WebhookContentType.WEBHOOK_CONTENT_TYPE_JSON;
    case 1:
    case 'WEBHOOK_CONTENT_TYPE_XML':
      return WebhookContentType.WEBHOOK_CONTENT_TYPE_XML;
    case -1:
    case 'UNRECOGNIZED':
    default:
      return WebhookContentType.UNRECOGNIZED;
  }
}

export function webhookContentTypeToJSON(object: WebhookContentType): string {
  switch (object) {
    case WebhookContentType.WEBHOOK_CONTENT_TYPE_JSON:
      return 'WEBHOOK_CONTENT_TYPE_JSON';
    case WebhookContentType.WEBHOOK_CONTENT_TYPE_XML:
      return 'WEBHOOK_CONTENT_TYPE_XML';
    case WebhookContentType.UNRECOGNIZED:
    default:
      return 'UNRECOGNIZED';
  }
}

export enum WebhookMethod {
  WEBHOOK_METHOD_GET = 0,
  WEBHOOK_METHOD_PUT = 1,
  WEBHOOK_METHOD_PATCH = 2,
  WEBHOOK_METHOD_POST = 3,
  WEBHOOK_METHOD_DELETE = 4,
  UNRECOGNIZED = -1,
}

export function webhookMethodFromJSON(object: any): WebhookMethod {
  switch (object) {
    case 0:
    case 'WEBHOOK_METHOD_GET':
      return WebhookMethod.WEBHOOK_METHOD_GET;
    case 1:
    case 'WEBHOOK_METHOD_PUT':
      return WebhookMethod.WEBHOOK_METHOD_PUT;
    case 2:
    case 'WEBHOOK_METHOD_PATCH':
      return WebhookMethod.WEBHOOK_METHOD_PATCH;
    case 3:
    case 'WEBHOOK_METHOD_POST':
      return WebhookMethod.WEBHOOK_METHOD_POST;
    case 4:
    case 'WEBHOOK_METHOD_DELETE':
      return WebhookMethod.WEBHOOK_METHOD_DELETE;
    case -1:
    case 'UNRECOGNIZED':
    default:
      return WebhookMethod.UNRECOGNIZED;
  }
}

export function webhookMethodToJSON(object: WebhookMethod): string {
  switch (object) {
    case WebhookMethod.WEBHOOK_METHOD_GET:
      return 'WEBHOOK_METHOD_GET';
    case WebhookMethod.WEBHOOK_METHOD_PUT:
      return 'WEBHOOK_METHOD_PUT';
    case WebhookMethod.WEBHOOK_METHOD_PATCH:
      return 'WEBHOOK_METHOD_PATCH';
    case WebhookMethod.WEBHOOK_METHOD_POST:
      return 'WEBHOOK_METHOD_POST';
    case WebhookMethod.WEBHOOK_METHOD_DELETE:
      return 'WEBHOOK_METHOD_DELETE';
    case WebhookMethod.UNRECOGNIZED:
    default:
      return 'UNRECOGNIZED';
  }
}

export interface WebhookList {
  webhooks: Webhook[];
}

export interface DataCollection {
  webhooks: { [key: string]: WebhookList };
}

export interface DataCollection_WebhooksEntry {
  key: string;
  value: WebhookList | undefined;
}

export interface Webhook {
  createdAt: Date | undefined;
  archivedAt: Date | undefined;
  lastUpdatedAt: Date | undefined;
  name: string;
  url: string;
  method: WebhookMethod;
  id: string;
  belongsToAccount: string;
  contentType: WebhookContentType;
  triggerConfigs: WebhookTriggerConfig[];
  createdByUser: string;
}

/** WebhookTriggerConfig is the join-table record (webhook subscription to a trigger event). */
export interface WebhookTriggerConfig {
  createdAt: Date | undefined;
  archivedAt: Date | undefined;
  id: string;
  belongsToWebhook: string;
  triggerEventId: string;
}

/** WebhookTriggerEvent is the catalog entity for available trigger event types. */
export interface WebhookTriggerEvent {
  id: string;
  name: string;
  description: string;
  createdAt: Date | undefined;
  lastUpdatedAt: Date | undefined;
  archivedAt: Date | undefined;
}

function createBaseWebhookList(): WebhookList {
  return { webhooks: [] };
}

export const WebhookList: MessageFns<WebhookList> = {
  encode(message: WebhookList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.webhooks) {
      Webhook.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WebhookList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWebhookList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.webhooks.push(Webhook.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WebhookList {
    return {
      webhooks: globalThis.Array.isArray(object?.webhooks) ? object.webhooks.map((e: any) => Webhook.fromJSON(e)) : [],
    };
  },

  toJSON(message: WebhookList): unknown {
    const obj: any = {};
    if (message.webhooks?.length) {
      obj.webhooks = message.webhooks.map((e) => Webhook.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WebhookList>, I>>(base?: I): WebhookList {
    return WebhookList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WebhookList>, I>>(object: I): WebhookList {
    const message = createBaseWebhookList();
    message.webhooks = object.webhooks?.map((e) => Webhook.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDataCollection(): DataCollection {
  return { webhooks: {} };
}

export const DataCollection: MessageFns<DataCollection> = {
  encode(message: DataCollection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.webhooks).forEach(([key, value]: [string, WebhookList]) => {
      DataCollection_WebhooksEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataCollection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataCollection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = DataCollection_WebhooksEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.webhooks[entry1.key] = entry1.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataCollection {
    return {
      webhooks: isObject(object.webhooks)
        ? (globalThis.Object.entries(object.webhooks) as [string, any][]).reduce(
            (acc: { [key: string]: WebhookList }, [key, value]: [string, any]) => {
              acc[key] = WebhookList.fromJSON(value);
              return acc;
            },
            {},
          )
        : {},
    };
  },

  toJSON(message: DataCollection): unknown {
    const obj: any = {};
    if (message.webhooks) {
      const entries = globalThis.Object.entries(message.webhooks) as [string, WebhookList][];
      if (entries.length > 0) {
        obj.webhooks = {};
        entries.forEach(([k, v]) => {
          obj.webhooks[k] = WebhookList.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataCollection>, I>>(base?: I): DataCollection {
    return DataCollection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataCollection>, I>>(object: I): DataCollection {
    const message = createBaseDataCollection();
    message.webhooks = (globalThis.Object.entries(object.webhooks ?? {}) as [string, WebhookList][]).reduce(
      (acc: { [key: string]: WebhookList }, [key, value]: [string, WebhookList]) => {
        if (value !== undefined) {
          acc[key] = WebhookList.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseDataCollection_WebhooksEntry(): DataCollection_WebhooksEntry {
  return { key: '', value: undefined };
}

export const DataCollection_WebhooksEntry: MessageFns<DataCollection_WebhooksEntry> = {
  encode(message: DataCollection_WebhooksEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== '') {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      WebhookList.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataCollection_WebhooksEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataCollection_WebhooksEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = WebhookList.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataCollection_WebhooksEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : '',
      value: isSet(object.value) ? WebhookList.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: DataCollection_WebhooksEntry): unknown {
    const obj: any = {};
    if (message.key !== '') {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = WebhookList.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataCollection_WebhooksEntry>, I>>(base?: I): DataCollection_WebhooksEntry {
    return DataCollection_WebhooksEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataCollection_WebhooksEntry>, I>>(object: I): DataCollection_WebhooksEntry {
    const message = createBaseDataCollection_WebhooksEntry();
    message.key = object.key ?? '';
    message.value =
      object.value !== undefined && object.value !== null ? WebhookList.fromPartial(object.value) : undefined;
    return message;
  },
};

function createBaseWebhook(): Webhook {
  return {
    createdAt: undefined,
    archivedAt: undefined,
    lastUpdatedAt: undefined,
    name: '',
    url: '',
    method: 0,
    id: '',
    belongsToAccount: '',
    contentType: 0,
    triggerConfigs: [],
    createdByUser: '',
  };
}

export const Webhook: MessageFns<Webhook> = {
  encode(message: Webhook, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(10).fork()).join();
    }
    if (message.archivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.archivedAt), writer.uint32(18).fork()).join();
    }
    if (message.lastUpdatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUpdatedAt), writer.uint32(26).fork()).join();
    }
    if (message.name !== '') {
      writer.uint32(34).string(message.name);
    }
    if (message.url !== '') {
      writer.uint32(42).string(message.url);
    }
    if (message.method !== 0) {
      writer.uint32(48).int32(message.method);
    }
    if (message.id !== '') {
      writer.uint32(58).string(message.id);
    }
    if (message.belongsToAccount !== '') {
      writer.uint32(66).string(message.belongsToAccount);
    }
    if (message.contentType !== 0) {
      writer.uint32(72).int32(message.contentType);
    }
    for (const v of message.triggerConfigs) {
      WebhookTriggerConfig.encode(v!, writer.uint32(82).fork()).join();
    }
    if (message.createdByUser !== '') {
      writer.uint32(90).string(message.createdByUser);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Webhook {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWebhook();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.archivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lastUpdatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.method = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.belongsToAccount = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.contentType = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.triggerConfigs.push(WebhookTriggerConfig.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.createdByUser = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Webhook {
    return {
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      archivedAt: isSet(object.archivedAt)
        ? fromJsonTimestamp(object.archivedAt)
        : isSet(object.archived_at)
          ? fromJsonTimestamp(object.archived_at)
          : undefined,
      lastUpdatedAt: isSet(object.lastUpdatedAt)
        ? fromJsonTimestamp(object.lastUpdatedAt)
        : isSet(object.last_updated_at)
          ? fromJsonTimestamp(object.last_updated_at)
          : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      url: isSet(object.url) ? globalThis.String(object.url) : '',
      method: isSet(object.method) ? webhookMethodFromJSON(object.method) : 0,
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      belongsToAccount: isSet(object.belongsToAccount)
        ? globalThis.String(object.belongsToAccount)
        : isSet(object.belongs_to_account)
          ? globalThis.String(object.belongs_to_account)
          : '',
      contentType: isSet(object.contentType)
        ? webhookContentTypeFromJSON(object.contentType)
        : isSet(object.content_type)
          ? webhookContentTypeFromJSON(object.content_type)
          : 0,
      triggerConfigs: globalThis.Array.isArray(object?.triggerConfigs)
        ? object.triggerConfigs.map((e: any) => WebhookTriggerConfig.fromJSON(e))
        : globalThis.Array.isArray(object?.trigger_configs)
          ? object.trigger_configs.map((e: any) => WebhookTriggerConfig.fromJSON(e))
          : [],
      createdByUser: isSet(object.createdByUser)
        ? globalThis.String(object.createdByUser)
        : isSet(object.created_by_user)
          ? globalThis.String(object.created_by_user)
          : '',
    };
  },

  toJSON(message: Webhook): unknown {
    const obj: any = {};
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.archivedAt !== undefined) {
      obj.archivedAt = message.archivedAt.toISOString();
    }
    if (message.lastUpdatedAt !== undefined) {
      obj.lastUpdatedAt = message.lastUpdatedAt.toISOString();
    }
    if (message.name !== '') {
      obj.name = message.name;
    }
    if (message.url !== '') {
      obj.url = message.url;
    }
    if (message.method !== 0) {
      obj.method = webhookMethodToJSON(message.method);
    }
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.belongsToAccount !== '') {
      obj.belongsToAccount = message.belongsToAccount;
    }
    if (message.contentType !== 0) {
      obj.contentType = webhookContentTypeToJSON(message.contentType);
    }
    if (message.triggerConfigs?.length) {
      obj.triggerConfigs = message.triggerConfigs.map((e) => WebhookTriggerConfig.toJSON(e));
    }
    if (message.createdByUser !== '') {
      obj.createdByUser = message.createdByUser;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Webhook>, I>>(base?: I): Webhook {
    return Webhook.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Webhook>, I>>(object: I): Webhook {
    const message = createBaseWebhook();
    message.createdAt = object.createdAt ?? undefined;
    message.archivedAt = object.archivedAt ?? undefined;
    message.lastUpdatedAt = object.lastUpdatedAt ?? undefined;
    message.name = object.name ?? '';
    message.url = object.url ?? '';
    message.method = object.method ?? 0;
    message.id = object.id ?? '';
    message.belongsToAccount = object.belongsToAccount ?? '';
    message.contentType = object.contentType ?? 0;
    message.triggerConfigs = object.triggerConfigs?.map((e) => WebhookTriggerConfig.fromPartial(e)) || [];
    message.createdByUser = object.createdByUser ?? '';
    return message;
  },
};

function createBaseWebhookTriggerConfig(): WebhookTriggerConfig {
  return { createdAt: undefined, archivedAt: undefined, id: '', belongsToWebhook: '', triggerEventId: '' };
}

export const WebhookTriggerConfig: MessageFns<WebhookTriggerConfig> = {
  encode(message: WebhookTriggerConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(10).fork()).join();
    }
    if (message.archivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.archivedAt), writer.uint32(18).fork()).join();
    }
    if (message.id !== '') {
      writer.uint32(26).string(message.id);
    }
    if (message.belongsToWebhook !== '') {
      writer.uint32(34).string(message.belongsToWebhook);
    }
    if (message.triggerEventId !== '') {
      writer.uint32(42).string(message.triggerEventId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WebhookTriggerConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWebhookTriggerConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.archivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.belongsToWebhook = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.triggerEventId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WebhookTriggerConfig {
    return {
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      archivedAt: isSet(object.archivedAt)
        ? fromJsonTimestamp(object.archivedAt)
        : isSet(object.archived_at)
          ? fromJsonTimestamp(object.archived_at)
          : undefined,
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      belongsToWebhook: isSet(object.belongsToWebhook)
        ? globalThis.String(object.belongsToWebhook)
        : isSet(object.belongs_to_webhook)
          ? globalThis.String(object.belongs_to_webhook)
          : '',
      triggerEventId: isSet(object.triggerEventId)
        ? globalThis.String(object.triggerEventId)
        : isSet(object.trigger_event_id)
          ? globalThis.String(object.trigger_event_id)
          : '',
    };
  },

  toJSON(message: WebhookTriggerConfig): unknown {
    const obj: any = {};
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.archivedAt !== undefined) {
      obj.archivedAt = message.archivedAt.toISOString();
    }
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.belongsToWebhook !== '') {
      obj.belongsToWebhook = message.belongsToWebhook;
    }
    if (message.triggerEventId !== '') {
      obj.triggerEventId = message.triggerEventId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WebhookTriggerConfig>, I>>(base?: I): WebhookTriggerConfig {
    return WebhookTriggerConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WebhookTriggerConfig>, I>>(object: I): WebhookTriggerConfig {
    const message = createBaseWebhookTriggerConfig();
    message.createdAt = object.createdAt ?? undefined;
    message.archivedAt = object.archivedAt ?? undefined;
    message.id = object.id ?? '';
    message.belongsToWebhook = object.belongsToWebhook ?? '';
    message.triggerEventId = object.triggerEventId ?? '';
    return message;
  },
};

function createBaseWebhookTriggerEvent(): WebhookTriggerEvent {
  return { id: '', name: '', description: '', createdAt: undefined, lastUpdatedAt: undefined, archivedAt: undefined };
}

export const WebhookTriggerEvent: MessageFns<WebhookTriggerEvent> = {
  encode(message: WebhookTriggerEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== '') {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== '') {
      writer.uint32(26).string(message.description);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(34).fork()).join();
    }
    if (message.lastUpdatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUpdatedAt), writer.uint32(42).fork()).join();
    }
    if (message.archivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.archivedAt), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WebhookTriggerEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWebhookTriggerEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.lastUpdatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.archivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WebhookTriggerEvent {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      description: isSet(object.description) ? globalThis.String(object.description) : '',
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      lastUpdatedAt: isSet(object.lastUpdatedAt)
        ? fromJsonTimestamp(object.lastUpdatedAt)
        : isSet(object.last_updated_at)
          ? fromJsonTimestamp(object.last_updated_at)
          : undefined,
      archivedAt: isSet(object.archivedAt)
        ? fromJsonTimestamp(object.archivedAt)
        : isSet(object.archived_at)
          ? fromJsonTimestamp(object.archived_at)
          : undefined,
    };
  },

  toJSON(message: WebhookTriggerEvent): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.name !== '') {
      obj.name = message.name;
    }
    if (message.description !== '') {
      obj.description = message.description;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.lastUpdatedAt !== undefined) {
      obj.lastUpdatedAt = message.lastUpdatedAt.toISOString();
    }
    if (message.archivedAt !== undefined) {
      obj.archivedAt = message.archivedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WebhookTriggerEvent>, I>>(base?: I): WebhookTriggerEvent {
    return WebhookTriggerEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WebhookTriggerEvent>, I>>(object: I): WebhookTriggerEvent {
    const message = createBaseWebhookTriggerEvent();
    message.id = object.id ?? '';
    message.name = object.name ?? '';
    message.description = object.description ?? '';
    message.createdAt = object.createdAt ?? undefined;
    message.lastUpdatedAt = object.lastUpdatedAt ?? undefined;
    message.archivedAt = object.archivedAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === 'string') {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === 'object' && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
