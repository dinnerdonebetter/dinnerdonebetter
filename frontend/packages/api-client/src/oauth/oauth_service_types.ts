// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.1
// source: oauth/oauth_service_types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { ResponseDetails } from '../common';
import { Pagination, QueryFilter } from '../filtering';
import { OAuth2Client } from './oauth_messages';

export const protobufPackage = 'oauth';

export interface CreateOAuth2ClientRequest {
  input: OAuth2ClientCreationRequestInput | undefined;
}

export interface CreateOAuth2ClientResponse {
  responseDetails: ResponseDetails | undefined;
  created: OAuth2Client | undefined;
}

export interface ArchiveOAuth2ClientRequest {
  oauth2ClientId: string;
}

export interface ArchiveOAuth2ClientResponse {
  responseDetails: ResponseDetails | undefined;
}

export interface GetOAuth2ClientRequest {
  oauth2ClientId: string;
}

export interface GetOAuth2ClientResponse {
  responseDetails: ResponseDetails | undefined;
  result: OAuth2Client | undefined;
}

export interface GetOAuth2ClientsRequest {
  filter: QueryFilter | undefined;
}

export interface GetOAuth2ClientsResponse {
  responseDetails: ResponseDetails | undefined;
  pagination: Pagination | undefined;
  results: OAuth2Client[];
}

export interface OAuth2ClientCreationRequestInput {
  name: string;
  description: string;
}

export interface OAuth2ClientCreationResponse {
  clientId: string;
  clientSecret: string;
  name: string;
  description: string;
  id: string;
}

function createBaseCreateOAuth2ClientRequest(): CreateOAuth2ClientRequest {
  return { input: undefined };
}

export const CreateOAuth2ClientRequest: MessageFns<CreateOAuth2ClientRequest> = {
  encode(message: CreateOAuth2ClientRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.input !== undefined) {
      OAuth2ClientCreationRequestInput.encode(message.input, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateOAuth2ClientRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateOAuth2ClientRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.input = OAuth2ClientCreationRequestInput.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateOAuth2ClientRequest {
    return { input: isSet(object.input) ? OAuth2ClientCreationRequestInput.fromJSON(object.input) : undefined };
  },

  toJSON(message: CreateOAuth2ClientRequest): unknown {
    const obj: any = {};
    if (message.input !== undefined) {
      obj.input = OAuth2ClientCreationRequestInput.toJSON(message.input);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateOAuth2ClientRequest>, I>>(base?: I): CreateOAuth2ClientRequest {
    return CreateOAuth2ClientRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateOAuth2ClientRequest>, I>>(object: I): CreateOAuth2ClientRequest {
    const message = createBaseCreateOAuth2ClientRequest();
    message.input =
      object.input !== undefined && object.input !== null
        ? OAuth2ClientCreationRequestInput.fromPartial(object.input)
        : undefined;
    return message;
  },
};

function createBaseCreateOAuth2ClientResponse(): CreateOAuth2ClientResponse {
  return { responseDetails: undefined, created: undefined };
}

export const CreateOAuth2ClientResponse: MessageFns<CreateOAuth2ClientResponse> = {
  encode(message: CreateOAuth2ClientResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.created !== undefined) {
      OAuth2Client.encode(message.created, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateOAuth2ClientResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateOAuth2ClientResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.created = OAuth2Client.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateOAuth2ClientResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      created: isSet(object.created) ? OAuth2Client.fromJSON(object.created) : undefined,
    };
  },

  toJSON(message: CreateOAuth2ClientResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.created !== undefined) {
      obj.created = OAuth2Client.toJSON(message.created);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateOAuth2ClientResponse>, I>>(base?: I): CreateOAuth2ClientResponse {
    return CreateOAuth2ClientResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateOAuth2ClientResponse>, I>>(object: I): CreateOAuth2ClientResponse {
    const message = createBaseCreateOAuth2ClientResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.created =
      object.created !== undefined && object.created !== null ? OAuth2Client.fromPartial(object.created) : undefined;
    return message;
  },
};

function createBaseArchiveOAuth2ClientRequest(): ArchiveOAuth2ClientRequest {
  return { oauth2ClientId: '' };
}

export const ArchiveOAuth2ClientRequest: MessageFns<ArchiveOAuth2ClientRequest> = {
  encode(message: ArchiveOAuth2ClientRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.oauth2ClientId !== '') {
      writer.uint32(10).string(message.oauth2ClientId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveOAuth2ClientRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveOAuth2ClientRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.oauth2ClientId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArchiveOAuth2ClientRequest {
    return {
      oauth2ClientId: isSet(object.oauth2ClientId)
        ? globalThis.String(object.oauth2ClientId)
        : isSet(object.oauth2_client_id)
          ? globalThis.String(object.oauth2_client_id)
          : '',
    };
  },

  toJSON(message: ArchiveOAuth2ClientRequest): unknown {
    const obj: any = {};
    if (message.oauth2ClientId !== '') {
      obj.oauth2ClientId = message.oauth2ClientId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArchiveOAuth2ClientRequest>, I>>(base?: I): ArchiveOAuth2ClientRequest {
    return ArchiveOAuth2ClientRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArchiveOAuth2ClientRequest>, I>>(object: I): ArchiveOAuth2ClientRequest {
    const message = createBaseArchiveOAuth2ClientRequest();
    message.oauth2ClientId = object.oauth2ClientId ?? '';
    return message;
  },
};

function createBaseArchiveOAuth2ClientResponse(): ArchiveOAuth2ClientResponse {
  return { responseDetails: undefined };
}

export const ArchiveOAuth2ClientResponse: MessageFns<ArchiveOAuth2ClientResponse> = {
  encode(message: ArchiveOAuth2ClientResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveOAuth2ClientResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveOAuth2ClientResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArchiveOAuth2ClientResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
    };
  },

  toJSON(message: ArchiveOAuth2ClientResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArchiveOAuth2ClientResponse>, I>>(base?: I): ArchiveOAuth2ClientResponse {
    return ArchiveOAuth2ClientResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArchiveOAuth2ClientResponse>, I>>(object: I): ArchiveOAuth2ClientResponse {
    const message = createBaseArchiveOAuth2ClientResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    return message;
  },
};

function createBaseGetOAuth2ClientRequest(): GetOAuth2ClientRequest {
  return { oauth2ClientId: '' };
}

export const GetOAuth2ClientRequest: MessageFns<GetOAuth2ClientRequest> = {
  encode(message: GetOAuth2ClientRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.oauth2ClientId !== '') {
      writer.uint32(10).string(message.oauth2ClientId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetOAuth2ClientRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetOAuth2ClientRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.oauth2ClientId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetOAuth2ClientRequest {
    return {
      oauth2ClientId: isSet(object.oauth2ClientId)
        ? globalThis.String(object.oauth2ClientId)
        : isSet(object.oauth2_client_id)
          ? globalThis.String(object.oauth2_client_id)
          : '',
    };
  },

  toJSON(message: GetOAuth2ClientRequest): unknown {
    const obj: any = {};
    if (message.oauth2ClientId !== '') {
      obj.oauth2ClientId = message.oauth2ClientId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetOAuth2ClientRequest>, I>>(base?: I): GetOAuth2ClientRequest {
    return GetOAuth2ClientRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetOAuth2ClientRequest>, I>>(object: I): GetOAuth2ClientRequest {
    const message = createBaseGetOAuth2ClientRequest();
    message.oauth2ClientId = object.oauth2ClientId ?? '';
    return message;
  },
};

function createBaseGetOAuth2ClientResponse(): GetOAuth2ClientResponse {
  return { responseDetails: undefined, result: undefined };
}

export const GetOAuth2ClientResponse: MessageFns<GetOAuth2ClientResponse> = {
  encode(message: GetOAuth2ClientResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.result !== undefined) {
      OAuth2Client.encode(message.result, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetOAuth2ClientResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetOAuth2ClientResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.result = OAuth2Client.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetOAuth2ClientResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      result: isSet(object.result) ? OAuth2Client.fromJSON(object.result) : undefined,
    };
  },

  toJSON(message: GetOAuth2ClientResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.result !== undefined) {
      obj.result = OAuth2Client.toJSON(message.result);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetOAuth2ClientResponse>, I>>(base?: I): GetOAuth2ClientResponse {
    return GetOAuth2ClientResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetOAuth2ClientResponse>, I>>(object: I): GetOAuth2ClientResponse {
    const message = createBaseGetOAuth2ClientResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.result =
      object.result !== undefined && object.result !== null ? OAuth2Client.fromPartial(object.result) : undefined;
    return message;
  },
};

function createBaseGetOAuth2ClientsRequest(): GetOAuth2ClientsRequest {
  return { filter: undefined };
}

export const GetOAuth2ClientsRequest: MessageFns<GetOAuth2ClientsRequest> = {
  encode(message: GetOAuth2ClientsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filter !== undefined) {
      QueryFilter.encode(message.filter, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetOAuth2ClientsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetOAuth2ClientsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filter = QueryFilter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetOAuth2ClientsRequest {
    return { filter: isSet(object.filter) ? QueryFilter.fromJSON(object.filter) : undefined };
  },

  toJSON(message: GetOAuth2ClientsRequest): unknown {
    const obj: any = {};
    if (message.filter !== undefined) {
      obj.filter = QueryFilter.toJSON(message.filter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetOAuth2ClientsRequest>, I>>(base?: I): GetOAuth2ClientsRequest {
    return GetOAuth2ClientsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetOAuth2ClientsRequest>, I>>(object: I): GetOAuth2ClientsRequest {
    const message = createBaseGetOAuth2ClientsRequest();
    message.filter =
      object.filter !== undefined && object.filter !== null ? QueryFilter.fromPartial(object.filter) : undefined;
    return message;
  },
};

function createBaseGetOAuth2ClientsResponse(): GetOAuth2ClientsResponse {
  return { responseDetails: undefined, pagination: undefined, results: [] };
}

export const GetOAuth2ClientsResponse: MessageFns<GetOAuth2ClientsResponse> = {
  encode(message: GetOAuth2ClientsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDetails !== undefined) {
      ResponseDetails.encode(message.responseDetails, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      Pagination.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    for (const v of message.results) {
      OAuth2Client.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetOAuth2ClientsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetOAuth2ClientsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDetails = ResponseDetails.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = Pagination.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.results.push(OAuth2Client.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetOAuth2ClientsResponse {
    return {
      responseDetails: isSet(object.responseDetails)
        ? ResponseDetails.fromJSON(object.responseDetails)
        : isSet(object.response_details)
          ? ResponseDetails.fromJSON(object.response_details)
          : undefined,
      pagination: isSet(object.pagination) ? Pagination.fromJSON(object.pagination) : undefined,
      results: globalThis.Array.isArray(object?.results)
        ? object.results.map((e: any) => OAuth2Client.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetOAuth2ClientsResponse): unknown {
    const obj: any = {};
    if (message.responseDetails !== undefined) {
      obj.responseDetails = ResponseDetails.toJSON(message.responseDetails);
    }
    if (message.pagination !== undefined) {
      obj.pagination = Pagination.toJSON(message.pagination);
    }
    if (message.results?.length) {
      obj.results = message.results.map((e) => OAuth2Client.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetOAuth2ClientsResponse>, I>>(base?: I): GetOAuth2ClientsResponse {
    return GetOAuth2ClientsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetOAuth2ClientsResponse>, I>>(object: I): GetOAuth2ClientsResponse {
    const message = createBaseGetOAuth2ClientsResponse();
    message.responseDetails =
      object.responseDetails !== undefined && object.responseDetails !== null
        ? ResponseDetails.fromPartial(object.responseDetails)
        : undefined;
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? Pagination.fromPartial(object.pagination)
        : undefined;
    message.results = object.results?.map((e) => OAuth2Client.fromPartial(e)) || [];
    return message;
  },
};

function createBaseOAuth2ClientCreationRequestInput(): OAuth2ClientCreationRequestInput {
  return { name: '', description: '' };
}

export const OAuth2ClientCreationRequestInput: MessageFns<OAuth2ClientCreationRequestInput> = {
  encode(message: OAuth2ClientCreationRequestInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== '') {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== '') {
      writer.uint32(18).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OAuth2ClientCreationRequestInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOAuth2ClientCreationRequestInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OAuth2ClientCreationRequestInput {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      description: isSet(object.description) ? globalThis.String(object.description) : '',
    };
  },

  toJSON(message: OAuth2ClientCreationRequestInput): unknown {
    const obj: any = {};
    if (message.name !== '') {
      obj.name = message.name;
    }
    if (message.description !== '') {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OAuth2ClientCreationRequestInput>, I>>(
    base?: I,
  ): OAuth2ClientCreationRequestInput {
    return OAuth2ClientCreationRequestInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OAuth2ClientCreationRequestInput>, I>>(
    object: I,
  ): OAuth2ClientCreationRequestInput {
    const message = createBaseOAuth2ClientCreationRequestInput();
    message.name = object.name ?? '';
    message.description = object.description ?? '';
    return message;
  },
};

function createBaseOAuth2ClientCreationResponse(): OAuth2ClientCreationResponse {
  return { clientId: '', clientSecret: '', name: '', description: '', id: '' };
}

export const OAuth2ClientCreationResponse: MessageFns<OAuth2ClientCreationResponse> = {
  encode(message: OAuth2ClientCreationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientId !== '') {
      writer.uint32(10).string(message.clientId);
    }
    if (message.clientSecret !== '') {
      writer.uint32(18).string(message.clientSecret);
    }
    if (message.name !== '') {
      writer.uint32(26).string(message.name);
    }
    if (message.description !== '') {
      writer.uint32(34).string(message.description);
    }
    if (message.id !== '') {
      writer.uint32(42).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OAuth2ClientCreationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOAuth2ClientCreationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clientSecret = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OAuth2ClientCreationResponse {
    return {
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
          ? globalThis.String(object.client_id)
          : '',
      clientSecret: isSet(object.clientSecret)
        ? globalThis.String(object.clientSecret)
        : isSet(object.client_secret)
          ? globalThis.String(object.client_secret)
          : '',
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      description: isSet(object.description) ? globalThis.String(object.description) : '',
      id: isSet(object.id) ? globalThis.String(object.id) : '',
    };
  },

  toJSON(message: OAuth2ClientCreationResponse): unknown {
    const obj: any = {};
    if (message.clientId !== '') {
      obj.clientId = message.clientId;
    }
    if (message.clientSecret !== '') {
      obj.clientSecret = message.clientSecret;
    }
    if (message.name !== '') {
      obj.name = message.name;
    }
    if (message.description !== '') {
      obj.description = message.description;
    }
    if (message.id !== '') {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OAuth2ClientCreationResponse>, I>>(base?: I): OAuth2ClientCreationResponse {
    return OAuth2ClientCreationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OAuth2ClientCreationResponse>, I>>(object: I): OAuth2ClientCreationResponse {
    const message = createBaseOAuth2ClientCreationResponse();
    message.clientId = object.clientId ?? '';
    message.clientSecret = object.clientSecret ?? '';
    message.name = object.name ?? '';
    message.description = object.description ?? '';
    message.id = object.id ?? '';
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
