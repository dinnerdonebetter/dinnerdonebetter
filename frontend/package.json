{
  "name": "frontend",
  "private": true,
  "workspaces": [
    "consumer",
    "admin",
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "pin": "npm exec --workspaces --include-workspace-root -- npd -u",
    "pin:check": "npm run pin && git diff --exit-code -- .",
    "lint": "npm run pin:check && npm run lint -w consumer -w admin",
    "lint:fix": "npm run lint:fix -w consumer -w admin",
    "test": "npm run test:run -w consumer && npm run test:run -w admin",
    "check": "npm run check -w consumer && npm run check -w admin",
    "build": "npm run build -w consumer && npm run build -w admin",
    "dev:consumer": "npm run dev -w consumer",
    "dev:admin": "npm run dev -w admin"
  },
  "devDependencies": {
    "@smarlhens/npm-pin-dependencies": "0.11.1",
    "prettier": "3.8.1",
    "prettier-plugin-svelte": "3.5.1"
  }
}
